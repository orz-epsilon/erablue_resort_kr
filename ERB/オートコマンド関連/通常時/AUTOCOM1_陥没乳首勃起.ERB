;------------------------------------------------------------------------------
;うふふ終了後
;陥没乳首勃起状態だとしばらく勃起したままで快Bを与え続ける
;------------------------------------------------------------------------------
@AUTOCOM1
CALL SOURCE_CALC_快Ｂ(AUTOCOM_実行番号保存, -1, 50)
TCVAR:AUTOCOM_実行番号保存:陥没乳首露出フラグ = MAX(0, TCVAR:AUTOCOM_実行番号保存:陥没乳首露出フラグ - 50)

RETURN 1

@AUTOCOM_ABLE1(キャラ番号)
#DIM キャラ番号
;ターゲットが陥没乳首
SIF !TALENT:キャラ番号:陥没乳首
	RETURN 0
;乳首勃起中
SIF !TCVAR:キャラ番号:陥没乳首露出フラグ
	RETURN 0
;常時勃起中は連続発動しないように条件をつける
SIF GETBIT(TALENT:キャラ番号:発情期あり, 7) && CFLAG:キャラ番号:発情期フラグ < 0 && TCVAR:キャラ番号:陥没乳首露出フラグ <= 100
	RETURN 0
;乳首名札中も連続発動しないように条件をつける
SIF TEQUIP:キャラ番号:上半身下着あり < -1 && TCVAR:キャラ番号:陥没乳首露出フラグ <= 100
	RETURN 0
;うふふ中ではない
SIF CFLAG:キャラ番号:うふふ
	RETURN 0
RETURN 1

@MESSAGE_AUTOCOM1
PRINTFORM 평소에는 숨겨져 있던 %CALLNAME:AUTOCOM_実行番号保存%의 젖꼭지는,
SELECTCASE TEQUIP:上半身下着あり
	CASE -1
		PRINTL 안전핀에 꿰뚫려 숨는 것을 허락받지 못하고,
	CASE -2
		PRINTL 관통한 피어스 때문에 강제적으로 끌려 나온 채다.
	CASEELSE
		PRINTL 아직 발기한 채로 있는 것 같다.
ENDSELECT
PRINTFORML 문득 스치는 순간에 마찰되어 달콤하고 나른한 자극을 %CALLNAME:AUTOCOM_実行番号保存%에게 전하고 있다…



