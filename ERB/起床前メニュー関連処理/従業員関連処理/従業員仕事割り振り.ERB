@従業員仕事割り振り
#DIM 従業員一覧, 500
#DIM ソート配列, 500
#DIM ページ数
#DIM 変更キャラ
#DIMS 屋号保存
#DIM ソートタイプ
#DIMS 改行判定文字列
#DIM 空き店舗あり
#DIMS 常駐業務名
#DIM 固有仕事数
#DIMS 固有仕事配列, 50
#DIMS 固有仕事名
#DIM 固有仕事キー

変更キャラ = -1

$表示ループ
VARSET 従業員一覧, -1
VARSET ソート配列
VARSET 仕事人数表示用

DRAWLINE
PRINTL 직원이 할 일의 설정을 합니다.
PRINTL ※개발 중이므로, 일이 올바르게 작동하지 않을 수 있습니다.
DRAWLINE
LOCALS = <div rect='81,31,1937,3762' border='31' bcolor='#C0C0C0'></div><div rect='2050,710,3875,3080' border='31' bcolor='#C0C0C0'></div>

;現在いる従業員を配列へ
従業員一覧:0 = 0
LOCAL:1 = 1
仕事人数表示用:(現在仕事:0:0) += 1
ソート配列:0 = 現在仕事:0:0 * 100 + 現在仕事:0:1 + 1
FOR LOCAL, 1, CHARANUM
	SIF TALENT:LOCAL:従業員 == 0
		CONTINUE
	従業員一覧:(LOCAL:1) = LOCAL
	SIF 現在仕事:LOCAL:0 >= 0 && 現在仕事:LOCAL:0 < 100000
		仕事人数表示用:(現在仕事:(従業員一覧:(LOCAL:1)):0) += 1
	IF 現在仕事:LOCAL:0 == -1
		ソート配列:(LOCAL:1) = 99999
	ELSE
		ソート配列:(LOCAL:1) = 現在仕事:LOCAL:0 * 100 + 現在仕事:LOCAL:1 + 1
	ENDIF
	LOCAL:1 += 1
NEXT
IF ソートタイプ
	ARRAYMSORT ソート配列, 従業員一覧
ENDIF

;一覧表示
LOCALS += "<div rect='156,156,1737,3662'><nobr>"
FOR LOCAL, ページ数 * 17, ページ数 * 17 + 17
	IF 従業員一覧:LOCAL < 0
		LOCALS += "<br>"
		CONTINUE
	ENDIF
	IF 現在仕事:(従業員一覧:LOCAL):0 == -1
		LOCALS += @"<font color='#%カラーパレット_HTML("グレーアウト")%'><nonbutton>%CALLNAME:(従業員一覧:LOCAL), 16, LEFT%[상점 경영]</nonbutton></font>"
	ELSE
		SIF 変更キャラ == 従業員一覧:LOCAL
			LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>"
		LOCALS += @"<button value='{LOCAL}'>%CALLNAME:(従業員一覧:LOCAL), 16, LEFT%[%現在仕事数字文字列変換(現在仕事:(従業員一覧:LOCAL):0, 0, 従業員一覧:LOCAL)%"
		IF 現在仕事:(従業員一覧:LOCAL):0 == 2
			LOCALS = %SUBSTRING(LOCALS, 0, STRLENS(LOCALS) - 4)%
			IF 現在仕事:(従業員一覧:LOCAL):1 >= 10003 && 現在仕事:(従業員一覧:LOCAL):1 <= 10010
				屋号保存 = 『%空艇港店舗屋号記録:(現在仕事:(従業員一覧:LOCAL):1 % 10000)%』
			ELSE
				屋号保存 = 
			ENDIF
			LOCALS += @"：%MAP_GET("常駐業務名", REPLACE(GETPLACENAME(現在仕事:(従業員一覧:LOCAL):1 / 10000, 現在仕事:(従業員一覧:LOCAL):1 % 10000), "『.*』", ""))%%屋号保存%"
		ELSEIF (現在仕事:(従業員一覧:LOCAL):0 == 8 || 現在仕事:(従業員一覧:LOCAL):0 == 7)
			LOCALS += "："
			SIF 現在仕事:(従業員一覧:LOCAL):1 > 0
				LOCALS += @"%ERDNAME(知識素質, 現在仕事:(従業員一覧:LOCAL):1)%"
		ENDIF
		LOCALS += "]</button>"
		SIF 変更キャラ == 従業員一覧:LOCAL
			LOCALS += @"</font>"
	ENDIF
	LOCALS += "<br>"
NEXT
IF ページ数 > 0
	LOCALS += "<button value='1000'>　＜ 이전 페이지</button>"
ELSE
	LOCALS += "　　　　　　　 "
ENDIF
LOCALS += "　　　　"
IF 従業員一覧:(ページ数 * 17 + 17) > -1
	LOCALS += "<button value='1001'>다음 페이지 ＞</button>"
ENDIF
LOCALS += "<br>"
LOCALS += "<br>"
LOCALS += "<button value='999'>[999] 돌아가기</button>　　"
IF ソートタイプ
	LOCALS += "일 순서 <button value='998'>[전환]</button>"
ELSE
	LOCALS += "번호 순서 <button value='998'>[전환]</button>"
ENDIF
LOCALS += "<br>"
LOCALS += "</nobr></div>"



;仕事の種類と現在そこに従事している人数を表示
LOCALS += "<div rect='2125,770,3675,2812'><nobr>"
IF 変更キャラ > -1
	LOCALS += @"직원：%NAME表示(変更キャラ)%<br>"
	LOCALS += @"■%現在仕事数字文字列変換(現在仕事:変更キャラ:0, 0, 変更キャラ), 16, LEFT%<br><br>"
	LOCALS += @"%現在仕事数字文字列変換(現在仕事:変更キャラ:0, 1, 変更キャラ)%<br><br>"
	;仕事レベルが存在するなら表示
	IF 現在仕事:変更キャラ:0 == 2 && 現在仕事:変更キャラ:1 > 0
		常駐業務名 '= MAP_GET("常駐業務名", REPLACE(GETPLACENAME(現在仕事:変更キャラ:1 / 10000, 現在仕事:変更キャラ:1 % 10000), "『.*』", ""))
		LOCALS += @"현재의 %常駐業務名韓国語変換(常駐業務名)% 레벨：{常駐業務レベル:変更キャラ:(GETNUM(常駐業務レベル, 常駐業務名))}"
	ELSEIF 仕事レベル存在チェック(現在仕事:変更キャラ:0)
		LOCALS += @"현재의%現在仕事数字文字列変換(現在仕事:変更キャラ:0, 0, 変更キャラ)%레벨：{仕事レベル:変更キャラ:(現在仕事:変更キャラ:0)}"
	ELSEIF 現在仕事:変更キャラ:0 == 9
		LOCALS += @"현재 지식 소질：요리사 레벨：{知識素質:変更キャラ:料理人}"
	ELSE
		LOCALS += @"직업 레벨 없음"
	ENDIF
ELSE
	LOCALS += "직업 내용과 종사 인원 목록<br><br>"
	FOR LOCAL, 0, 50
		IF 現在仕事数字文字列変換(LOCAL) != ""
			LOCALS += @"■%現在仕事数字文字列変換(LOCAL), 16, LEFT%：종사 인원{仕事人数表示用:LOCAL,3}명<br>"
		ENDIF
	NEXT
ENDIF
LOCALS += "</nobr></div>"

;仕事切り替え用枠
LOCALS += "<div rect='2050,31,3875,650' padding='50,25,50' border='31' bcolor='#C0C0C0'>"
IF 変更キャラ > -1
	改行判定文字列 = 
	FOR LOCAL, 0, 50
		IF 現在仕事数字文字列変換(LOCAL) != ""
			SIF 仕事条件判定関数(変更キャラ, LOCAL) == 0
				CONTINUE
			IF LOCAL > 0
				IF STRLENS(改行判定文字列 + 現在仕事数字文字列変換(LOCAL)) > 75
					LOCALS += "<br>"
					改行判定文字列 = 
				ELSE
					LOCALS += " / "
					改行判定文字列 += " / "
				ENDIF
			ENDIF
			IF 現在仕事:変更キャラ:0 == LOCAL
				LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[%現在仕事数字文字列変換(LOCAL)%]</font>"
				改行判定文字列 += @"[%現在仕事数字文字列変換(LOCAL)%]"
			ELSE
				LOCALS += @"<button value='{LOCAL + 2000}'>[%現在仕事数字文字列変換(LOCAL)%]</button>"
				改行判定文字列 += @"[%現在仕事数字文字列変換(LOCAL)%]"
			ENDIF
		ENDIF
	NEXT

	VARSET RESULTS
	固有仕事数 = ENUMFUNCBEGINSWITH(@"固有仕事設定_{NO:変更キャラ}_")
	ARRAYCOPY "RESULTS", "固有仕事配列"
	IF 固有仕事数 > 0
		FOR LOCAL, 0, 固有仕事数
			SIF GETMETH(固有仕事配列:LOCAL, 0, 変更キャラ, "条件判定") == 0
				CONTINUE
			CALLFORMF %固有仕事配列:LOCAL%(変更キャラ, "仕事名")
			固有仕事名 '= TSTR:コマンド名受渡
			固有仕事キー = TOINT(REPLACE(固有仕事配列:LOCAL, @"固有仕事設定_{NO:変更キャラ}_", ""))
			SIF 固有仕事キー < 0
				CONTINUE
			IF STRLENS(改行判定文字列 + 固有仕事名) > 75
				LOCALS += "<br>"
				改行判定文字列 = 
			ELSE
				LOCALS += " / "
				改行判定文字列 += " / "
			ENDIF
			IF 現在仕事:変更キャラ:0 == 固有仕事キー + 100000
				LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[%固有仕事名%]</font>"
				改行判定文字列 += @"[%固有仕事名%]"
			ELSE
				LOCALS += @"<button value='{固有仕事キー + 100000}'>[%固有仕事名%]</button>"
				改行判定文字列 += @"[%固有仕事名%]"
			ENDIF
		NEXT
	ENDIF

	空き店舗あり = 0
	;空き店舗を検索
	FOR LOCAL, 0, VARSIZE("商業区店割り配列", 1)
		IF 商業区店割り配列:LOCAL:店舗名 == "" || 商業区店割り配列:LOCAL:店員配列 == "-1_"
			空き店舗あり = 1
			BREAK
		ENDIF
	NEXT
	IF 空き店舗あり && 港改造度:0:0 && TALENT:変更キャラ:定住者 && EXISTFUNCTION(@"特殊店舗_建設条件_{NO:変更キャラ}")
		LOCALS += "<br><button value='997'>[상점을 열게 하다]</button>"
	ENDIF
ENDIF
LOCALS += "</div>"



HTML_PRINT LOCALS
FOR LOCAL, 0, 34
	PRINTL
NEXT
DRAWLINE

BINPUT

LOCAL = RESULT
SELECTCASE LOCAL
	CASE IS >= 100000
		現在仕事:変更キャラ:0 = LOCAL
		現在仕事:変更キャラ:1 = 0
	CASE IS >= 3000
		CALL 仕事追加処理関数(変更キャラ, LOCAL)
	CASE IS >= 2000
		現在仕事:変更キャラ:0 = LOCAL - 2000
		現在仕事:変更キャラ:1 = 0
	CASE 1300 TO 1308
		INVERTBIT 秘書起床業務:変更キャラ:0, LOCAL - 1300
	CASE 1000
		ページ数 -= 1
	CASE 1001
		ページ数 += 1
	CASE 999
		CALL 仕事切替時補正(変更キャラ)
		ページ数 = 0
		IF 高速船記録:派遣先登録 != ""
			CALLFORM 高速船派遣先関数_%高速船記録:派遣先登録%("派遣条件")
			IF RESULT == 0
				CALLFORM 高速船派遣先関数_%高速船記録:派遣先登録%("解説文")
				DRAWLINE
				HTML_PRINT TSTR:ツールチップ受渡
				PRINTL
				DRAWLINE
				PRINTL 더 이상 고속선 파견 조건을 충족하지 못하므로, 파견을 중지합니다.
				PRINTL 괜찮습니까?
				PRINTBUTTONLC "[0] 예", 0
				PRINTBUTTONLC "[1] 아니오", 1
				PRINTL
				BINPUT
				IF RESULT == 0
					高速船記録:派遣先登録 = 
					高速船記録:派遣状態 = 
					PRINTL
					PRINTL
					PRINTW 파견을 중지했습니다.
				ELSE
					GOTO 表示ループ
				ENDIF
			ENDIF
		ENDIF
		RETURN 0
	CASE 998
		INVERTBIT ソートタイプ, 0
	CASE 997
		PRINTL
		PRINTL
		DRAWLINE
		PRINTL ※※※※※주의※※※※※
		PRINTFORML %CALLNAME:変更キャラ%이(가) 상업 지구에 점포를 열게 됩니다.
		PRINTL 앞으로, 통상적인 업무에 종사할 수 없게 됩니다.
		PRINTL 특수 점포가 준비된 캐릭터만 이 선택지가 나타나지만,
		PRINTL 점포를 열기 조건이 갖춰져 있는지 여부는 별개이므로 주의해 주십시오.
		DRAWLINE
		PRINTL
		PRINTFORML %NAME表示(変更キャラ)%에게 상점을 열도록 권유하시겠습니까?
		PRINTBUTTONLC "[0] 예", 0
		PRINTBUTTONLC "[1] 아니오", 1
		PRINTL
		BINPUT
		IF RESULT == 0
			PRINTFORMW %NAME表示(変更キャラ)%에게 상점을 열기 업무를 명했습니다.
			現在仕事:変更キャラ:0 = -1
			RETURN 0
		ELSE
			RESTART
		ENDIF
	CASE 0 TO 499
		CALL 仕事切替時補正(変更キャラ)
		変更キャラ = 従業員一覧:LOCAL
		;CALL 従業員仕事割り振り_切り替え(従業員一覧:LOCAL)
ENDSELECT

GOTO 表示ループ

@仕事切替時補正(対象キャラ)
#DIM 対象キャラ

SIF 対象キャラ < 0
	RETURN 0

;キャラ切り替え時や戻るボタンを押した時、設定必須の項目が設定されてなかった場合
;仕事をなしに戻す
IF 現在仕事:対象キャラ:0 == 2 && 現在仕事:対象キャラ:1 == 0
	現在仕事:対象キャラ:0 = 0
ENDIF
IF 現在仕事:対象キャラ:0 == 7
	SIF 現在仕事:対象キャラ:1 == 0
		現在仕事:対象キャラ:0 = 0
	IF 知識素質:対象キャラ:(現在仕事:対象キャラ:1) > 1
		現在仕事:対象キャラ:0 = 0
		現在仕事:対象キャラ:1 = 0
	ENDIF
ENDIF
IF 現在仕事:対象キャラ:0 == 8
	SIF 現在仕事:対象キャラ:1 == 0
		現在仕事:対象キャラ:0 = 0
	IF 知識素質:対象キャラ:(現在仕事:対象キャラ:1) < 2
		現在仕事:対象キャラ:0 = 0
		現在仕事:対象キャラ:1 = 0
	ENDIF
ENDIF

@従業員仕事割り振り_切り替え(ARG)
#DIM 空き店舗あり

DRAWLINE
PRINTL 종업원이 수행할 직업 설정을 합니다.
PRINTL ※개발 중이므로, 업무가 올바르게 작동하지 않을 수 있습니다.
DRAWLINE
LOCALS = <div rect='81,31,1937,2812' border='31' bcolor='#C0C0C0'></div><div rect='2050,31,3875,2812' border='31' bcolor='#C0C0C0'></div>
LOCALS += "<div rect='156,156,1937,2812'>"
LOCALS += @"■%CALLNAME:ARG%의 업무를 <br>"
FOR LOCAL, 0, 50
	IF 現在仕事数字文字列変換(LOCAL) != ""
		SIF 仕事条件判定関数(ARG, LOCAL) == 0
			CONTINUE
		IF 現在仕事:ARG:0 == LOCAL
			LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[{LOCAL}] %現在仕事数字文字列変換(LOCAL), 14, LEFT%</font><br>"
		ELSE
			LOCALS += @"<button value='{LOCAL}'>[{LOCAL}] %現在仕事数字文字列変換(LOCAL), 14, LEFT%으로 변경</button><br>"
		ENDIF
	ENDIF
NEXT
LOCALS += "<br>"

RESULT = 0
RESULTS = 
空き店舗あり = 0
;空き店舗を検索
FOR LOCAL, 0, VARSIZE("商業区店割り配列", 1)
	IF 商業区店割り配列:LOCAL:店舗名 == "" || 商業区店割り配列:LOCAL:店員配列 == "-1_"
		空き店舗あり = 1
		BREAK
	ENDIF
NEXT
IF 空き店舗あり && 港改造度:0:0 && TALENT:ARG:定住者 && EXISTFUNCTION(@"特殊店舗_建設条件_{NO:ARG}")
	LOCALS += "<button value='998'>[998] 상점을 열게 한다</button><br>"
	LOCALS += "<br>"
ENDIF

LOCALS += "<button value='999'>[999] 돌아가기</button>"
LOCALS += "<br>"
LOCALS += "</div>"

;仕事の説明を表示
LOCALS:1 = <div rect='2125,156,3675,2812'>
LOCALS:1 += "현재 종사하고 있는 업무<br>"
LOCALS:1 += @"■%現在仕事数字文字列変換(現在仕事:ARG:0, 0, ARG), 16, LEFT%"
LOCALS:1 += "</div>"
LOCALS:1 += "<div rect='2312,437,3675,2812'>"
LOCALS:1 += @"<nobr>%現在仕事数字文字列変換(現在仕事:ARG:0, 1, ARG)%</nobr>"
LOCALS:1 += "<br><br>"
;仕事レベルが存在するなら表示
IF 仕事レベル存在チェック(現在仕事:ARG:0)
	LOCALS:1 += @"현재 %現在仕事数字文字列変換(現在仕事:ARG:0, 0, ARG)% 레벨：{仕事レベル:ARG:(現在仕事:ARG:0)}"
ELSEIF 現在仕事:ARG:0 == 9
	LOCALS:1 += @"현재 지식 소질：요리사 레벨：{知識素質:ARG:料理人}"
ELSE
	LOCALS:1 += @"업무 레벨 없음"
ENDIF
LOCALS:1 += "</div>"


HTML_PRINT LOCALS, 1
HTML_PRINT LOCALS:1, 1
FOR LOCAL, 0, 25
	PRINTL
NEXT
DRAWLINE

$INPUT_LOOP
BINPUT

LOCAL = RESULT
SELECTCASE LOCAL
	CASE 999
		IF 現在仕事:ARG:0 == 2 && 現在仕事:ARG:1 == 0
			現在仕事:ARG:0 = 0
		ENDIF
		IF 現在仕事:ARG:0 == 7
			SIF 現在仕事:ARG:1 == 0
				現在仕事:ARG:0 = 0
			IF 知識素質:ARG:(現在仕事:ARG:1) > 1
				現在仕事:ARG:0 = 0
				現在仕事:ARG:1 = 0
			ENDIF
		ENDIF
		IF 現在仕事:ARG:0 == 8
			SIF 現在仕事:ARG:1 == 0
				現在仕事:ARG:0 = 0
			IF 知識素質:ARG:(現在仕事:ARG:1) < 2
				現在仕事:ARG:0 = 0
				現在仕事:ARG:1 = 0
			ENDIF
		ENDIF
		RETURN 0
	CASE 998
		PRINTL
		PRINTL
		DRAWLINE
		PRINTL ※※※※※주의※※※※※
		PRINTFORML %CALLNAME:ARG%이(가) 상업 지구에 점포를 열게 됩니다.
		PRINTL 앞으로, 통상적인 업무에 종사할 수 없게 됩니다.
		PRINTL 특수 점포가 준비된 캐릭터만 이 선택지가 나오지만,
		PRINTL 점포를 열기 위한 조건이 갖춰져 있는지 여부는 별개이므로 주의해 주십시오.
		DRAWLINE
		PRINTL
		PRINTFORML %NAME表示(ARG)%에게 상점을 열도록 권유하시겠습니까?
		PRINTBUTTONLC "[0] 예", 0
		PRINTBUTTONLC "[1] 아니오", 1
		PRINTL
		BINPUT
		IF RESULT == 0
			PRINTFORMW %NAME表示(ARG)%에게 상점을 열도록 업무를 명했습니다.
			現在仕事:ARG:0 = -1
			RETURN 0
		ELSE
			RESTART
		ENDIF
	CASE 0 TO 11
		IF 仕事条件判定関数(ARG, LOCAL) == 0
			PRINTFORMW %CALLNAME:ARG%은(는) %現在仕事数字文字列変換(LOCAL)%의 직업을 가질 수 없습니다.
			REUSELASTLINE 
			GOTO INPUT_LOOP
		ENDIF
		現在仕事:ARG:0 = LOCAL
		現在仕事:ARG:1 = 0
		RESTART
	CASEELSE
		CALL 仕事追加処理関数(ARG, LOCAL)
		RESTART
ENDSELECT

@現在仕事数字文字列変換(ARG, ARG:1 = 0, キャラ番号 = 0)
#FUNCTIONS
#DIM キャラ番号
#DIMS 一時文字列

;固有業務
IF ARG >= 100000
	IF ARG:1
		CALLFORMF 固有仕事設定_{NO:キャラ番号}_{ARG - 100000}(キャラ番号, "仕事解説")
		RETURNF TSTR:ツールチップ受渡
	ENDIF
	CALLFORMF 固有仕事設定_{NO:キャラ番号}_{ARG - 100000}(キャラ番号, "仕事名")
	RETURNF TSTR:コマンド名受渡
ENDIF

SELECTCASE ARG
	CASE -1
		IF ARG:1
			RETURNF "항구 지역에서 상점을 경영하고 있는 상태. <br>효과: 특별히 없음<br>　　　일반적인 점포 소유 캐릭터와 동일하게 행동합니다."
		ENDIF
		RETURNF "상점 경영"
	CASE 0
		IF ARG:1
			RETURNF "특별히 일을 맡기지 않고 대기하고 있는 상태. <br>효과: 특별히 없음<br>　　　일반적인 손님과 동일하게 행동합니다."
		ENDIF
		RETURNF "없음"
	CASE 1
		IF ARG:1
			RETURNF "리조트 내의 자잘한 잡무를 처리하는 업무. <br>효과: 하루의 마지막에, 모든 장소의 잡무 게이지가<br>　　　［（仕事レベル＋１）＾２×１００］감소합니다. <br>　　　리조트 내를 무작위로 이동합니다."
		ENDIF
		RETURNF "잡무"
	CASE 2
		IF ARG:1
			LOCALS = 특정 장소에서 전문적인 일을 수행하는 업무. <br>효과: 하루의 마지막에, 루피가<br>　　　［滞在者数×（仕事レベル＋１）＾３］증가합니다.
			LOCALS += "<br><br>"
			IF 現在仕事:キャラ番号:1 == 2
				LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[3002] 로비 : 현재 상주 장소</font><br>"
			ELSE
				LOCALS += @"<button value='3002'>[3002] 로비 에서 로비 접수원으로 일합니다</button><br>"
			ENDIF
			IF 施設改造度:5:0
				IF 現在仕事:キャラ番号:1 == 503
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[3503] 바 구역 : 현재 상주 장소</font><br>"
				ELSE
					LOCALS += @"<button value='3503'>[3503] 바 구역 에서 바텐더로 일합니다</button><br>"
				ENDIF
			ENDIF
			IF 施設改造度:5:6
				IF 現在仕事:キャラ番号:1 == 506
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[3506] 도서실 : 현재 상주 장소</font><br>"
				ELSE
					LOCALS += @"<button value='3506'>[3506] 도서실 에서 사서로 일합니다</button><br>"
				ENDIF
			ENDIF
			IF 施設改造度:15:0
				IF 現在仕事:キャラ番号:1 == 1503
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[4503] 풀사이드 ： 현재 상주 장소</font><br>"
				ELSE
					LOCALS += @"<button value='4503'>[4503] 풀사이드 에서 감시원으로 일한다</button><br>"
				ENDIF
			ELSE
				IF 現在仕事:キャラ番号:1 == 15
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[3015] 수영장 ： 현재 상주 장소</font><br>"
				ELSE
					LOCALS += @"<button value='3015'>[3015] 수영장 에서 감시원으로 일한다</button><br>"
				ENDIF
			ENDIF
			IF 空艇港初来訪イベントフラグ
				FOR LOCAL, 3, 9
					IF 空艇港店舗状況:LOCAL:0 > 0
						CALLFORMF 空艇港店舗名表示_{空艇港店舗状況:LOCAL:0}(LOCAL)
						IF 現在仕事:キャラ番号:1 == LOCAL + 10000
							LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[{10000 + LOCAL}] 점포{LOCAL}:%TSTR:場所名受渡% ： 현재 상주 장소</font><br>"
						ELSE
							LOCALS += @"<button value='{10000 + LOCAL}'>[{10000 + LOCAL}] 점포{LOCAL}:%TSTR:場所名受渡% 에서 점원으로 일한다</button><br>"
						ENDIF
					ENDIF
				NEXT
			ENDIF
			IF 海改造度:6:0
				IF 現在仕事:キャラ番号:1 == 30103
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[30103] 어선단 ： 현재 상주 장소</font><br>"
				ELSE
					LOCALS += @"<button value='30103'>[30103] 어선단 에서 어부로 일한다</button><br>"
				ENDIF
			ENDIF
			IF 海改造度:6:1 || 海改造度:6:2
				IF 海改造度:6:1
					IF 海改造度:6:2
						一時文字列 = 유람선・해저 산책
					ELSE
						一時文字列 = 유람선
					ENDIF
				ELSE
					一時文字列 = 해저 산책
				ENDIF
				IF 現在仕事:キャラ番号:1 == 30104
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[30104] %一時文字列% ： 현재 상주 장소</font><br>"
				ELSE
					LOCALS += @"<button value='30104'>[30104] %一時文字列% 에서 씨 가이드로 일한다</button><br>"
				ENDIF
			ENDIF
			RETURNF LOCALS
		ENDIF
		RETURNF "상주 업무"
	CASE 3
		IF ARG:1
			LOCALS = %CALLNAME:PLAYER%의 보조를 하는 업무. <br>효과: 하루의 마지막에, 호감도 요소: 신뢰도가<br>　　　［（仕事レベル＋１）＾２×２００］증가한다. <br>　　　리조트 내를 무작위로 이동한다.
			;ここに秘書起床業務の切り替えボタンを仕込む
			LOCALS += "<br><br>"
			LOCALS += "자발적으로 해본 적 있는 기상 행위를 의뢰할 수 있다<br>（반드시 실행해 주는 것은 아니지만, 다른 기상 방법은 발생하기 어려워진다）<br>"
			;母乳体質が消えている場合、授乳は選択不可
			SIF GETBIT(秘書起床業務:キャラ番号:0, 起こし方_授乳) && !TALENT:キャラ番号:母乳体質
				CLEARBIT 秘書起床業務:キャラ番号:0, 起こし方_授乳
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_騎乗位), TOSTR(1300 + 起こし方_騎乗位), "질 봉사", "아침 발기를 기승위로 처리해 준다", 1, 射精可能(MASTER), !(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_騎乗位)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_Ａ騎乗位), TOSTR(1300 + 起こし方_Ａ騎乗位), "항문 봉사", "아침 발기를 A기승위로 처리해 준다", 1, 射精可能(MASTER), !(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_Ａ騎乗位)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_授乳), TOSTR(1300 + 起こし方_授乳), "수유", "아침 첫 젖을 짜서 먹여준다. 소질[모유 체질] 필요", 1, TALENT:キャラ番号:母乳体質, !(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_授乳)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_フェラチオ), TOSTR(1300 + 起こし方_フェラチオ), "구강 봉사", "아침 발기를 펠라치오로 처리해 준다", 1, 射精可能(MASTER), !(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_フェラチオ)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_パイズリ), TOSTR(1300 + 起こし方_パイズリ), "유방 봉사", "아침 발기를 파이즈리로 처리해 준다", 1 ,射精可能(MASTER), !(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_パイズリ)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_手コキ), TOSTR(1300 + 起こし方_手コキ), "수음 봉사", "아침 발기를 핸드잡으로 처리해 준다", 1 , 射精可能(MASTER), !(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_手コキ)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_ペッティング), TOSTR(1300 + 起こし方_ペッティング), "애무", "애무로 깨워준다", 1, 1,!(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_ペッティング)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_キス), TOSTR(1300 + 起こし方_キス), "키스", "키스해서 깨워준다", 1, 1, !(陥落チェック_性的(キャラ番号) && GETBIT(秘書起床経験:キャラ番号:0, 起こし方_キス)))
			LOCALS += HTMLボタン_条件付き(GETBIT(秘書起床業務:キャラ番号:0, 起こし方_ノーマル), TOSTR(1300 + 起こし方_ノーマル), "말 걸기", "방까지 깨우러 와준다", 1, 1)
			RETURNF LOCALS
		ENDIF
		RETURNF "비서"
	CASE 4
		IF ARG:1
			RETURNF @"편지나 광고 등으로 리조트를 선전하는 업무.<br>효과: 하루의 마지막에, 리조트의 인기도가<br>　　　［（仕事レベル＋１）＾２×１］증가한다.<br>　　　직원 방의 자기 방에서 전단지 만들기 등을 한다."
		ENDIF
		RETURNF "선전"
	CASE 5
		IF 施設改造度:3:8 == 0
			RETURNF ""
		ENDIF
		IF ARG:1
			RETURNF @"훈련장에서 전투 능력을 단련하는 업무.<br>효과: 하루의 마지막에 EXP를 획득한다.<br>　　　획득량은 모든 캐릭터 중 가장 높은 레벨에 따라 변동한다.<br>　　　(모든 캐릭터 중 가장 높은 레벨 이상으로는 성장하지 않는다)<br>　　　하루에 500kcal를 소비한다.<br>　　　훈련장에 상주한다."
		ENDIF
		RETURNF "훈련"
	CASE 6
		IF あなた特殊能力:ダンジョン探索PRO < 1
			RETURNF ""
		ENDIF
		IF ARG:1
			RETURNF @"클리어한 던전에 가서 소재를 모아오는 업무. <br>효과: 하루의 마지막에 소재와 EXP를 획득한다. <br>　　　획득량은 방문한 던전에 따라 변동한다. <br>　　　(방문할 던전 설정은 「탐색하러 가기」에서 한다)<br>　　　%CALLNAME:MASTER%이(가) 탐색을 하지 않는 날은 효과가 증가한다. <br>　　　낮에는 리조트에 없으며, 저녁 이후에는 자기 방에 있다. <br>　　　하루에 400kcal 소비한다. <br>　　　이 업무에는 업무 레벨이 존재하지 않는다."
		ENDIF
		RETURNF "자동 탐색"
	CASE 7
		IF 居住区改造度:0:0 == 0
			RETURNF ""
		ENDIF
		IF ARG:1
			LOCALS = 학교에 가서 지정된 지식에 대해 학습하는 업무. <br>효과: 오전과 오후 학습 종료 시 지식 경험치를 얻는다. <br>　　　학교에 없는 시간대에는 자기 방에 있다. <br>　　　Lv2에 도달한 지식은 선택할 수 없다.
			LOCALS += "<br><br>"
			LOCAL:1 = 0
			FOR LOCAL, 1, 100
				SIF ERDNAME(知識素質, LOCAL) == ""
					BREAK
				IF 知識素質:キャラ番号:LOCAL > 1
					LOCALS += @"<font color='#%カラーパレット_HTML("グレーアウト")%'>[{LOCAL + 3000}] %ERDNAME(知識素質, LOCAL), 9, LEFT%을(를) 배운다(현재Lv{知識素質:キャラ番号:LOCAL})</font>　　　"
				ELSEIF 現在仕事:キャラ番号:1 == LOCAL
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[{LOCAL + 3000}] %ERDNAME(知識素質, LOCAL), 9, LEFT%을(를) 배운다(현재Lv{知識素質:キャラ番号:LOCAL}) 선택</font>　"
				ELSE
					LOCALS += @"<button value='{LOCAL + 3000}'>[{LOCAL + 3000}] %ERDNAME(知識素質, LOCAL), 9, LEFT%을(를) 배운다(현재Lv{知識素質:キャラ番号:LOCAL})</button>　　　"
				ENDIF
				LOCAL:1 ++
				SIF LOCAL:1 % 2 == 0
					LOCALS += "<br>"
			NEXT
			RETURNF LOCALS
		ENDIF
		RETURNF "지식 습득"
	CASE 8
		IF 居住区改造度:0:0 == 0
			RETURNF ""
		ENDIF
		IF ARG:1
			LOCALS = 학교에 가서 지정된 지식에 대해 강의하는 업무. <br>효과: 학교에서 배우는 캐릭터의 학습 효율이 상승한다. <br>　　　학교에 없는 시간대에는 자기 방에 있다. <br>　　　Lv2 이상의 지식만 선택 가능.
			LOCALS += "<br><br>"
			LOCAL:1 = 0
			FOR LOCAL, 1, 100
				SIF ERDNAME(知識素質, LOCAL) == ""
					BREAK
				IF 知識素質:キャラ番号:LOCAL < 2
					LOCALS += @"<font color='#%カラーパレット_HTML("グレーアウト")%'>[{LOCAL + 3000}] %ERDNAME(知識素質, LOCAL), 9, LEFT% 교사(현재Lv{知識素質:キャラ番号:LOCAL})</font>　　　　"
				ELSEIF 現在仕事:キャラ番号:1 == LOCAL
					LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[{LOCAL + 3000}] %ERDNAME(知識素質, LOCAL), 9, LEFT% 교사(현재Lv{知識素質:キャラ番号:LOCAL}) 선택</font>　　"
				ELSE
					LOCALS += @"<button value='{LOCAL + 3000}'>[{LOCAL + 3000}] %ERDNAME(知識素質, LOCAL), 9, LEFT% 교사(현재Lv{知識素質:キャラ番号:LOCAL})</button>　　　　"
				ENDIF
				LOCAL:1 ++
				SIF LOCAL:1 % 2 == 0
					LOCALS += "<br>"
			NEXT
			RETURNF LOCALS
		ENDIF
		RETURNF "교사"
	CASE 9
		IF ARG:1
			RETURNF "리조트 내 식당에서 요리를 담당하는 업무. <br>효과: 요리 메뉴의 효과에 따른 ZP 수입이<br>　　　[(知識素質：料理人レベル＋1)✕5]% 증가한다. <br>　　　여러 명의 효과는 가산되지만, 상한은<br>　　　[체류 인원÷10]%까지. (몹·유니크 합산)<br>　　　料理人レベル이 마이너스인 캐릭터를 이 업무에 배치해서는 안 된다."
		ENDIF
		RETURNF "조리사"
	CASE 10
		IF ARG:1
			RETURNF "리조트 내 식당에서 급사를 담당하는 업무. <br>효과: 요리 메뉴의 효과에 따른 루피 수입이<br>　　　[(仕事レベル＋1)✕5]% 증가한다. <br>　　　여러 명의 효과는 가산되지만, 상한은<br>[체류 인원÷10]%까지. (몹·유니크 합산)"
		ENDIF
		RETURNF "급사"
	CASE 11
		IF ARG:1
			RETURNF @"고속선에 탑승하여 파견지로 향하는 업무. <br>효과: 파견지에 따라 변화."
		ENDIF
		RETURNF "고속선 선원"
	CASE 12
		IF ARG:1
			RETURNF "리조트를 습격하는 외적을 제거하는 업무. <br>효과: 하루의 마지막에 특정 이벤트가 발생했을 경우<br>　　　업무 레벨에 따른 확률로 이벤트를 해결한다. <br>　　　(여러 명 있을 경우, 확률은 가산)<br>　　　리조트 내를 무작위로 이동한다."
		ENDIF
		RETURNF "경비원"
ENDSELECT

@仕事条件判定関数(キャラ番号, 仕事番号)
#FUNCTION
#DIM キャラ番号
#DIM 仕事番号

SELECTCASE 仕事番号
	CASE 2,3,7,8,9,10,11
		SIF キャラ番号 == PLAYER
			RETURNF 0
	CASE 5,6,12
		SIF キャラ番号 == PLAYER
			RETURNF 0
		SIF TALENT:キャラ番号:非戦闘キャラ
			RETURNF 0
ENDSELECT

RETURNF 1


@仕事追加処理関数(キャラ番号, 選択番号)
#DIM キャラ番号
#DIM 選択番号

SELECTCASE 現在仕事:キャラ番号:0
	CASE 2
		;常駐する店舗を選べる
		IF 選択番号 > 10000
			現在仕事:キャラ番号:1 = 選択番号
		ELSE
			現在仕事:キャラ番号:1 = 選択番号 - 3000
		ENDIF
	CASE 7
		;知識を選べる
		現在仕事:キャラ番号:1 = 選択番号 - 3000
	CASE 8
		;知識を選べる
		現在仕事:キャラ番号:1 = 選択番号 - 3000
ENDSELECT

@仕事レベル存在チェック(チェック番号)
#FUNCTION
#DIM チェック番号

IF GROUPMATCH(チェック番号, 1, 2, 3, 4, 8, 10, 12)
	RETURNF 1
ELSE
	RETURNF 0
ENDIF
