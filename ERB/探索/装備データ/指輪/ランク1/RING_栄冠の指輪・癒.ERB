
@指輪個別文章表示_栄冠の指輪・癒
LOCALS = 
LOCALS += @"■영광의 반지・치유<br><br>"
LOCALS += "마력을 끌어내는 흔한 반지. <br>"
LOCALS += "회복 성능에 특화된 조정이 이루어져, 서포터 역할의 기공사에게 인기. <br>"
LOCALS += "범용품이므로 전 하늘에서 널리 사용되며, 던전에도 자주 떨어져 있다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 5000

@指輪基礎効果文章表示_栄冠の指輪・癒(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기본 효과】<br>"
LOCALS += "최대HP+100 최대MP+80 방어력+50 회복력+100 행동 속도+50<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_栄冠の指輪・癒(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 1
	RETURN 0
SELECTCASE 入手タイプ
	CASE "輸入"
		RETURN 装備レアリティ_Ｎ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_Ｎ
ENDSELECT

@指輪ステータス決定_栄冠の指輪・癒(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대HP_100_최대MP_80_방어력_50_회복력_100_행동속도_50

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 1
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv1のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(1)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "栄冠の指輪・癒", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
