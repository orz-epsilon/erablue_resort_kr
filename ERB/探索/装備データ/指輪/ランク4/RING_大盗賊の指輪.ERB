
@指輪個別文章表示_大盗賊の指輪
LOCALS = 
LOCALS += @"■대도적의 반지<br><br>"
LOCALS += "전설의 대도적이 장착했다고 전해지는 사연 있는 희귀한 반지.<br>"
LOCALS += "일반 공격 시, 드물게 그 대상에게 드롭률 UP 효과를 부여할 때가 있다.<br>"
LOCALS += "장비하고 있으면, 권력자나 상식에 반항하고 싶어지는… 기분이 든다.<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 75000

@指輪基礎効果文章表示_大盗賊の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기초 효과】<br>"
LOCALS += "최대HP+300 최대MP+300 공격력+300 행동 속도+600<br>"
LOCALS += "TA율+30% 명중률+10% 가끔 드롭률 UP<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_大盗賊の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 4
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＳＲ
ENDSELECT

@指輪ステータス決定_大盗賊の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대HP_300_최대MP_300_공격력_300_행동속도_600_TA율_30_명중률_10

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 3
ENDIF

;オプション数に応じてオプション効果をランダムで付加

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(3)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "大盗賊の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)


@固有通常攻撃後処理_指輪_大盗賊の指輪(全滅フラグ, 連撃数)
#DIM 全滅フラグ
#DIM 連撃数
#DIM ループ用

FOR ループ用, 0, 連撃数
	SIF RAND:3
		CONTINUE

	ARRAYCOPY "アビテンプレート用_通常攻撃使用先保存", "アビテンプレート用_対象保存"
	CALL アビ汎用変数文字列リセット
	CALL アビテンプレート_不利効果_トレハン
NEXT
