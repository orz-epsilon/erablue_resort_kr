
@指輪個別文章表示_魁の指輪
LOCALS = 
LOCALS += @"■괴수의 반지<br><br>"
LOCALS += "어떤 군대의 선두에서 가장 먼저 적진에 돌격하는 인물이 특별 주문했다는 반지.<br>"
LOCALS += "대열의 선두에 있는 인물에게만, 이 반지는 힘을 부여한다.<br>"
LOCALS += "그 성능은 그야말로 괴수와 같은 속공 특화.<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 60000

@指輪基礎効果文章表示_魁の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기본 효과】<br>"
LOCALS += "공격력+300 행동 속도+600 DA율+40% TA율+25% 명중률+10% ※조건 있음<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_魁の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 4
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＲ
ENDSELECT

@指輪基礎効果適用判定_魁の指輪(対象キャラ)
#DIM 対象キャラ
SIF BATTLE_STATE:0:キャラ登録番号 == 対象キャラ
	RETURN 1
RETURN 0

@指輪ステータス決定_魁の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 공격력_300_행동속도_600_DA율_40_TA율_25_명중률_10

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 3
ENDIF

;オプション数に応じてオプション効果をランダムで付加

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(3)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "魁の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
