
@指輪個別文章表示_覇業の指輪・速
LOCALS = 
LOCALS += @"■패업의 반지・속<br><br>"
LOCALS += "마력을 끌어내는 고품질의 반지. <br>"
LOCALS += "선수를 쳐서 수로 밀어붙이는 조정이 되어 있어, 속공을 선호하는 기공사에게 인기. <br>"
LOCALS += "범용품이기 때문에 전공에서 널리 사용되고 있으며, 던전에도 자주 떨어져 있다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 35000

@指輪基礎効果文章表示_覇業の指輪・速(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기초 효과】<br>"
LOCALS += "최대ＨＰ+200 최대ＭＰ+200 공격력+200 행동속도+400<br>"
LOCALS += "DA율+35% TA率+23% 명중률+10%<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_覇業の指輪・速(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 4
	RETURN 0
SELECTCASE 入手タイプ
	CASE "輸入"
		RETURN 装備レアリティ_Ｎ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_Ｎ
ENDSELECT

@指輪ステータス決定_覇業の指輪・速(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_200_最大ＭＰ_200_攻撃力_200_行動速度_400_DA율_35_TA率_23_命中率_10

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "覇業の指輪・速", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
