
@指輪個別文章表示_抗眠の指輪
LOCALS = 
LOCALS += @"■항면의 반지<br><br>"
LOCALS += "장비자를 수면으로부터 보호하는 반지. <br>"
LOCALS += "수면 마법을 가진 몬스터가 많은 지역에서 활동하는 기공사의 필수품. <br>"
LOCALS += "범용품이므로 전 공역에서 널리 사용되지만, 원료 문제로 다소 공급 부족 경향이 있다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 25000

@指輪基礎効果文章表示_抗眠の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기본 효과】<br>"
LOCALS += "최대 MP+200 수면 내성+100<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_抗眠の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 4
	RETURN 0
SELECTCASE 入手タイプ
	CASE "輸入"
		RETURN 装備レアリティ_Ｒ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_Ｒ
ENDSELECT

@指輪ステータス決定_抗眠の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＭＰ_200

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 1
ENDIF

;オプション数に応じてオプション効果をランダムで付加

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(1)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "抗眠の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)


@固有バフ・デバフ処理_指輪_抗眠の指輪_状態異常耐性_睡眠(隊列番号, 状態異常耐性)
#DIM 隊列番号
#DIM 状態異常耐性

RETURN 状態異常耐性 + 100
