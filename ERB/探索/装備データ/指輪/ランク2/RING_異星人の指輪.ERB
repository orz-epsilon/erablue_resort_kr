
@指輪個別文章表示_異星人の指輪
LOCALS = 
LOCALS += @"■외계인의 반지<br><br>"
LOCALS += "큰 가슴이야말로 궁극이라고 생각하는 외계인의 마력이 담긴 반지. <br>"
LOCALS += "그가 인정하는 체형의 소유자에게만, 이 반지는 힘을 빌려준다. <br>"
LOCALS += "그 중전차와 같은 파워는, 불리한 효과마저도 물리칠지도 모른다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 35000

@指輪基礎効果文章表示_異星人の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기본 효과】<br>"
LOCALS += "최대HP+300 공격력+300 방어력+300 약체 내성+15% ※조건 있음<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_異星人の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 2
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＲ
ENDSELECT

@指輪基礎効果適用判定_異星人の指輪(対象キャラ)
#DIM 対象キャラ
;男性はダメ
SIF TALENT:対象キャラ:性別 == 2
	RETURN 0
;Bが存在しないとダメ
SIF TALENT:対象キャラ:Ｂ感度 == -2
	RETURN 0
RETURN (TALENT:対象キャラ:バストサイズ == 2 || TALENT:対象キャラ:バストサイズ == 3)

@指輪ステータス決定_異星人の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대HP_300_공격력_300_방어력_300_약체 내성_15

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "異星人の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
