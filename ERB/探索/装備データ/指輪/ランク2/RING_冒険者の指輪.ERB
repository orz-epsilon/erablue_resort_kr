
@指輪個別文章表示_冒険者の指輪
LOCALS = 
LOCALS += @"■모험가의 반지<br><br>"
LOCALS += "마력을 끌어내는 성능 좋은 반지. <br>"
LOCALS += "모든 상황을 상정한 조정으로, 모든 기공사의 능력 향상을 기대할 수 있다. <br>"
LOCALS += "비용 면에서 채산성이 맞지 않아, 현재는 생산되지 않고 사용자도 적다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 75000

@指輪基礎効果文章表示_冒険者の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기초 효과】<br>"
LOCALS += "최대HP+400 최대MP+120 공격력+120 방어력+120 회복력+120<br>"
LOCALS += "행동 속도+120 DA율+10% TA율+5% 즉사 내성+30<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_冒険者の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 2
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＳＲ
ENDSELECT

@指輪ステータス決定_冒険者の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대HP_400_최대MP_120_공격력_120_방어력_120_회복력_120_행동속도_120_DA율_10_TA율_5

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "冒険者の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)


@固有バフ・デバフ処理_指輪_抗毒の指輪_状態異常耐性_即死(隊列番号, 状態異常耐性)
#DIM 隊列番号
#DIM 状態異常耐性

RETURN 状態異常耐性 + 30
