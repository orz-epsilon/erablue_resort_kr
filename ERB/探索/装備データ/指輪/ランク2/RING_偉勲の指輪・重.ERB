
@指輪個別文章表示_偉勲の指輪・重
LOCALS = 
LOCALS += @"■위훈의 반지・중<br><br>"
LOCALS += "마력을 끌어내는 성능 좋은 반지. <br>"
LOCALS += "전위에서 싸우기 위한 조정이 되어 있으며, 견실한 기공사에게 인기. <br>"
LOCALS += "범용품이므로 전 공역에서 널리 사용되며, 던전에도 자주 떨어져 있다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 15000

@指輪基礎効果文章表示_偉勲の指輪・重(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기본 효과】<br>"
LOCALS += "最大ＨＰ+200 最大ＭＰ+20 攻撃力+100 防御力+200 回復力+100<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_偉勲の指輪・重(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 2
	RETURN 0
SELECTCASE 入手タイプ
	CASE "輸入"
		RETURN 装備レアリティ_Ｎ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_Ｎ
ENDSELECT

@指輪ステータス決定_偉勲の指輪・重(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_200_最大ＭＰ_20_攻撃力_100_防御力_200_回復力_100

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "偉勲の指輪・重", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
