
@指輪個別文章表示_領民の指輪
LOCALS = 
LOCALS += @"■영민의 반지<br><br>"
LOCALS += "「백성에게 안녕이 찾아오기를」이라는 옛 영주의 염원이 담긴 반지.<br>"
LOCALS += "그 염원이 마력이 되어, 리조트 주민들에게 힘을 준다.<br>"
LOCALS += "성능은 방어에만 특화되어 있어, 호신 용도로는 든든하다.<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 30000

@指輪基礎効果文章表示_領民の指輪(指輪番号 = 0, 効果番号 = -1)
#DIM 効果番号
#DIM 指輪番号
IF 効果番号 > -1
	;効果を指定されているのでランダム部分固定
	LOCALS = 
	LOCALS += "【기초 효과】<br>"
	LOCALS += @"최대 HP+600 방어력+300 회복력+150 행동 속도+100 %属性数値文字列変換(効果番号 - 1)%내성+20 ※조건 있음<br>"
ELSEIF 指輪番号 < 1
	;所持してない指輪なのでランダム表記
	LOCALS = 
	LOCALS += "【기초 효과】<br>"
	LOCALS += "최대 HP+600 방어력+300 회복력+150 행동 속도+100 랜덤 속성 내성+20 ※조건 있음<br>"
ELSE
	;所持してる指輪なのでランダム部分を引っ張ってくる
	LOCALS = 
	LOCALS += "【기초 효과】<br>"
	LOCALS += "최대 HP+600 방어력+300 회복력+150 행동 속도+100 "
	LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "基礎ステータス補正")%
	LOCALS:1 = %SUBSTRING(LOCALS:1, 58, 2)%
	LOCALS += @"%LOCALS:1%내성+20 ※조건 있음<br>"
ENDIF

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%
;ランダム効果数を返す
RETURN 6

@指輪出現率_領民の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 2
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＲ
ENDSELECT

@指輪基礎効果適用判定_領民の指輪(対象キャラ)
#DIM 対象キャラ
SIF TALENT:対象キャラ:定住者 > 0
	RETURN 1
RETURN 0

@指輪ステータス決定_領民の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대 HP_600_방어력_300_회복력_150_행동 속도_100
IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF
IF 効果指定 > -1
	LOCAL = 効果指定
ELSE
	LOCAL = RAND:6
ENDIF
基本補正文字列 += @"_%属性数値文字列変換(LOCAL)%내성_20"

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "領民の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
