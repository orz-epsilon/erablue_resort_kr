
@指輪個別文章表示_精霊の指輪・火
LOCALS = 
LOCALS += @"■정령의 반지・불<br><br>"
LOCALS += "불의 상위 정령의 힘이 깃든 반지. <br>"
LOCALS += "공격 성능에 치중한 능력과, 불의 특성을 강화하는 힘을 지닌다. <br>"
LOCALS += "강적 「화령 바르톨로메우스」의 힘이 담긴 매우 희귀한 반지. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 15000

@指輪基礎効果文章表示_精霊の指輪・火(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기초 효과】<br>"
LOCALS += "최대 HP+350 공격력+450 행동 속도+400 불 강화+15 바람 내성+10<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

;ランダム抽選では出現しない（入手法：ダンジョン「バルツ地下廃工場」でボス「火霊バルトロメウス」を初回撃破）
;@指輪出現率_精霊の指輪・火(入手タイプ, 装備ランク)

@指輪ステータス決定_精霊の指輪・火(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_350_攻撃力_450_行動速度_400_火強化_15_風耐性_10

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 4
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv4のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(4)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "精霊の指輪・火", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
