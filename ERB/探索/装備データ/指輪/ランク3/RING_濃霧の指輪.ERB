
@指輪個別文章表示_濃霧の指輪
LOCALS = 
LOCALS += @"■짙은 안개의 반지<br><br>"
LOCALS += "전설의 살인귀가 착용했다고 알려진 사연 있는 희귀한 반지. <br>"
LOCALS += "인간형 상대에게 공격했을 때, 위력이 증가한다. <br>"
LOCALS += "밤이 되면, 장비자 주위에 안개가 발생하는 듯한 느낌이 든다……<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 100000

@指輪基礎効果文章表示_濃霧の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기본 효과】<br>"
LOCALS += "최대HP+400 최대MP+120 공격력+240 방어력+240 회복력+240<br>"
LOCALS += "행동 속도+240 DA율+10% TA율+5% 대인형 특공<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_濃霧の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 3
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＳＲ
ENDSELECT

@指輪ステータス決定_濃霧の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대HP_400_최대MP_120_공격력_240_방어력_240_회복력_240_행동속도_240_DA율_10_TA율_5

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 3
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv3のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(3)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "濃霧の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)


@固有バフ・デバフ処理_指輪_濃霧の指輪_与ダメージ(隊列番号, ダメージ値)
#DIM 隊列番号
#DIM ダメージ値

;対象の種族に人型があるなら
IF 種族判定関数(戦闘行動対象, "人型")
	;ダメージ１．５倍
	RETURN ダメージ値 * 150 / 100
ENDIF

;上に引っかからなかったらダメージをそのまま返す
RETURN ダメージ値
