
@指輪個別文章表示_拒絶の指輪
LOCALS = 
LOCALS += @"■거절의 반지<br><br>"
LOCALS += "공격을 튕겨내는 장벽을 만들어내는 반지. <br>"
LOCALS += "전투 시작 시 [500]의 배리어를 장비자에게 부여한다. <br>"
LOCALS += "계속되는 전투로 인한 소모를 억제할 수 있어 용병에게 인기. <br>"
LOCALS += "하지만 공격 성능은 낮아, 동료와 연계하는 것이 전제인 장비이다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 15000

@指輪基礎効果文章表示_拒絶の指輪(指輪番号 = 0, 効果番号 = -1)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기초 효과】<br>"
LOCALS += "최대HP+200 방어력+300 회복력+100 행동 속도+150 배리어 생성<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_拒絶の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 3
	RETURN 0
SELECTCASE 入手タイプ
	CASE "輸入"
		RETURN 装備レアリティ_Ｒ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_Ｒ
ENDSELECT

@指輪ステータス決定_拒絶の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대HP_200_방어력_300_회복력_100_행동속도_150
IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "拒絶の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)


@固有戦闘開始時処理_指輪_拒絶の指輪

CALL アビ対象選択テンプレート_自己のみ
アビテンプレート用_アビ名 = 拒絶の指輪
アビ汎用変数:効果量 = 500
アビ汎用文字列:バフ・デバフ枠 = バリア
アビ汎用文字列:バフ・デバフ対象能力 = バリア
アビ汎用変数:持続ターン = -1
CALL アビテンプレート_有利効果_バフ効果セット
