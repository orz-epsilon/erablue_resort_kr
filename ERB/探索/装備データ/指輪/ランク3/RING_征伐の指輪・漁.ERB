
@指輪個別文章表示_征伐の指輪・漁
LOCALS = 
LOCALS += @"■정벌의 반지・어<br><br>"
LOCALS += "마력을 끌어내는 성능이 좋은 반지. <br>"
LOCALS += "토벌 대상을 상정한 조정이 되어 있으며, 사전에 작전을 세우는 기공사에게 인기. <br>"
LOCALS += "범용품이므로 전 공역에서 널리 사용되고 있으며, 던전에도 자주 떨어져 있다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 25000

@指輪基礎効果文章表示_征伐の指輪・漁(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기초 효과】<br>"
LOCALS += "최대 HP+400 최대 MP+120 공격력+120 방어력+120 회복력+120<br>"
LOCALS += "행동 속도+120 DA율+10% TA率+5% 대 수서・물고기・게 특공<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_征伐の指輪・漁(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 3
	RETURN 0
SELECTCASE 入手タイプ
	CASE "輸入"
		RETURN 装備レアリティ_Ｎ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_Ｎ
ENDSELECT

@指輪ステータス決定_征伐の指輪・漁(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_400_最大ＭＰ_120_攻撃力_120_防御力_120_回復力_120_行動速度_120_DA率_10_TA率_5

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "征伐の指輪・漁", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)


@固有バフ・デバフ処理_指輪_征伐の指輪・漁_与ダメージ(隊列番号, ダメージ値)
#DIM 隊列番号
#DIM ダメージ値

;対象の種族がいずれかの特攻条件を満たすなら
IF 種族判定関数(戦闘行動対象, "水棲", "魚類", "カニ")
	;ダメージ１．３倍
	RETURN ダメージ値 * 130 / 100
ENDIF

;上に引っかからなかったらダメージをそのまま返す
RETURN ダメージ値
