
@指輪個別文章表示_朱夏の指輪
LOCALS = 
LOCALS += @"■주하의 반지<br><br>"
LOCALS += "어떤 나라의 무녀가 화행의 기운을 담아 만든 반지. <br>"
LOCALS += "여름에 장비함으로써 높은 공격 성능을 발휘한다. <br>"
LOCALS += "힘차게 인생을 구가하는 어른의 장비에도 적합할 것 같다. <br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 50000

@指輪基礎効果文章表示_朱夏の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【기초 효과】<br>"
LOCALS += "최대HP+450 최대MP+90 공격력+400 방어력+60 행동 속도+200 ※조건 있음<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【옵션 효과】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_朱夏の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 3
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＲ
ENDSELECT

@指輪基礎効果適用判定_朱夏の指輪(対象キャラ)
#DIM 対象キャラ
;夏なら無条件でOK
SIF (DAY / 30) % 4 == 1
	RETURN 1
;耳飾りとのセット効果
SIF GROUPMATCH(DT_CELL_GETS("所持耳飾りデータベース", 装備ステータス補正保存:対象キャラ:装備耳飾り番号, "耳飾り名", 1), "桜花の耳飾り", "朝顔の耳飾り", "紅葉の耳飾り", "水仙の耳飾り", "蓮華の耳飾り", "菊花の耳飾り") > 0
	RETURN 1
;大人・壮年
RETURN (TALENT:対象キャラ:年齢層 == 年齢層_大人 || TALENT:対象キャラ:年齢層 == 年齢層_壮年)

@指輪ステータス決定_朱夏の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 최대HP_450_최대MP_90_공격력_400_방어력_60_행동속도_200

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "朱夏の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
