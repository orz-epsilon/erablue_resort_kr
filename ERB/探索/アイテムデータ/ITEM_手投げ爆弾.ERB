
@アイテム個別文章表示_手投げ爆弾(ARGS)
VARSET LOCALS
CALL 口上変数初期化

IF ARGS == "値段"
	詳細文文字列受け渡し変数 = 루피
	RETURN 300
ENDIF


LOCALS += @"■투척 폭탄　　　소지 수:{DT_CELL_GET("所持アイテムデータベース", 0, "手投げ爆弾")}<br>"
LOCALS += "조합한 폭발물을 채워 넣은 간이 폭탄. <br>"
LOCALS += "마을 사람 등이 호신용으로 지니는 타입의 것. <br>"
LOCALS += "폭발 범위는 좁고 위력도 낮지만 약한 마물에게는 충분. <br>"
LOCALS += "<br>"
LOCALS:1 += "【효과】<br>"
LOCALS:1 += "적 단일 대상에게 [１００]의 고정 대미지를 준다. <br>"

詳細文文字列受け渡し変数 = %LOCALS%%LOCALS:1%
KSTR:(K++) = %LOCALS:1%

@アイテム使用先表示_手投げ爆弾
CALL 汎用使用先表示("敵単体")

@アイテム効果処理_手投げ爆弾(ARG)
;ARG = エネミーが何番目か、10からカウント
CALL 口上変数初期化

アビ汎用変数:効果量 = 100
CALL アビテンプレート_ダメージ処理_無属性

SIF アビテンプレート用_キャンセルフラグ
	RETURN -1

RETURN 1
