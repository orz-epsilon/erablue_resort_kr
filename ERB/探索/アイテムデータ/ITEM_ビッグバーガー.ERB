
@アイテム個別文章表示_ビッグバーガー(ARGS)
;ビッグバーガー
IF ARGS == "値段"
	詳細文文字列受け渡し変数 = 루피
	RETURN 1480
ENDIF

LOCALS = 
LOCALS += @"■빅 버거　　　소지 수:{DT_CELL_GET("所持アイテムデータベース", 0, "ビッグバーガー")}<br>"
LOCALS += "고기, 야채, 계란 프라이를 듬뿍 넣은 맛있는 버거.<br>"
LOCALS += "드라프 남성도 만족할 만한 거대한 사이즈.<br>"
LOCALS += "먹으면 기운이 솟아나 체력과 마력이 회복된다.<br>"
LOCALS += "전투 중에는 사용할 수 없다.<br>"
LOCALS += "섭취 칼로리:525kcal<br>"
LOCALS += "<br>"
LOCALS += "【효과】<br>"
LOCALS += "아군 단일 대상의 HP를 [1000], MP를 [150] 회복한다.<br>"

詳細文文字列受け渡し変数 = %LOCALS%

@アイテム使用先表示_ビッグバーガー
;戦闘時使用不可
CALL 汎用使用先表示("味方単体", -1)

@アイテム効果処理_ビッグバーガー(ARG)
#DIM CONST HP回復量 = 1000
#DIM CONST MP回復量 = 150
;ARG = 隊列の何番目か

IF !BATTLE_STATE:ARG:キャラ登録番号
	RETURN 0
ENDIF

IF DT_CELL_GET("所持アイテムデータベース", 0, "ビッグバーガー") < 1
	PRINTW 아이템이 없습니다
	RETURN 0
ENDIF

IF BATTLE_STATE:ARG:최대HP == BATTLE_STATE:ARG:ＨＰ && BATTLE_STATE:ARG:최대MP == BATTLE_STATE:ARG:ＭＰ
	PRINTW HP와 MP가 최대입니다
	RETURN 0
ENDIF

IF BATTLE_STATE:ARG:ＨＰ < 1
	PRINTW 전투 불능은 치료할 수 없습니다
	RETURN 0
ENDIF

IF BATTLE_STATE:ARG:최대HP - BATTLE_STATE:ARG:ＨＰ < HP回復量
	PRINTFORMW %CALLNAME:(BATTLE_STATE:ARG:キャラ登録番号)%의 HP를 {BATTLE_STATE:ARG:최대HP - BATTLE_STATE:ARG:ＨＰ}회복했습니다
ELSE
	PRINTFORMW %CALLNAME:(BATTLE_STATE:ARG:キャラ登録番号)%의 HP를 {HP回復量}회복했습니다
ENDIF
IF BATTLE_STATE:ARG:최대MP - BATTLE_STATE:ARG:ＭＰ < MP回復量
	PRINTFORMW %CALLNAME:(BATTLE_STATE:ARG:キャラ登録番号)%의 MP를 {BATTLE_STATE:ARG:최대MP - BATTLE_STATE:ARG:ＭＰ}회복했습니다
ELSE
	PRINTFORMW %CALLNAME:(BATTLE_STATE:ARG:キャラ登録番号)%의 MP를 {MP回復量}회복했습니다
ENDIF

BATTLE_STATE:ARG:ＨＰ = MIN(BATTLE_STATE:ARG:ＨＰ + HP回復量, BATTLE_STATE:ARG:최대HP)
BATTLE_STATE:ARG:ＭＰ = MIN(BATTLE_STATE:ARG:ＭＰ + MP回復量, BATTLE_STATE:ARG:최대MP)
TCVAR:(BATTLE_STATE:ARG:キャラ登録番号):摂取カロリー += 525

RETURN 1
