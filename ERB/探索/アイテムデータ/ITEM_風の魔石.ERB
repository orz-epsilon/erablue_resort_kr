
@アイテム個別文章表示_風の魔石(ARGS)
VARSET LOCALS
CALL 口上変数初期化

IF ARGS == "値段"
	詳細文文字列受け渡し変数 = 루피
	RETURN 2000
ENDIF


LOCALS += @"■바람의 마석　　　소지 수:{DT_CELL_GET("所持アイテムデータベース", 0, "風の魔石")}<br>"
;LOCALS +="－－－－－－－－－－－－－－－－－－－－－－－－－－－－<br>" 改行目安
LOCALS += "바람의 마력을 많이 포함한 마석.<br>"
LOCALS += "기동하면 부근의 마력에 따른 돌풍을 분출시킨다.<br>"
LOCALS += "위력은 낮지만 효과 범위가 넓어, 다수를 상대할 때 유용한 공<br>"
LOCALS += "격 도구이다.<br>"
LOCALS += "<br>"
LOCALS:1 += "【효과】<br>"
LOCALS:1 += "적 전체에게[PT의 최대 레벨✕２０]의 대미지를 준다.<br>"

詳細文文字列受け渡し変数 = %LOCALS%%LOCALS:1%
KSTR:(K++) = %LOCALS:1%

@アイテム使用先表示_風の魔石
CALL 汎用使用先表示("敵全体")

@アイテム効果処理_風の魔石(ARG)
#DIM レベル記録
;ARG = エネミーが何番目か、10からカウント
CALL 口上変数初期化

レベル記録 = 0
FOR LOCAL, 0, 14
	SIF LOCAL > 3 && LOCAL < 10
		CONTINUE
	SIF BATTLE_STATE:LOCAL:キャラ登録番号 < 1
		CONTINUE
	SIF BATTLE_STATE:LOCAL:Lv > レベル記録
		レベル記録 = BATTLE_STATE:LOCAL:Lv
NEXT

アビ汎用変数:効果量 = レベル記録 * 20
CALL アビテンプレート_ダメージ処理_風属性

SIF アビテンプレート用_キャンセルフラグ
	RETURN -1

RETURN 1
