
;-------------------------------------------------
;村役場で条例を発布する
;-------------------------------------------------
@COMNAME_PLACE_380_2_9
#FUNCTION
TSTR:コマンド名受渡 = 조례를 발포하다
RETURNF 1
;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_380_2_9
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("単独可能")
		
@COM380_2_9
#DIM 画面表示用変数
DRAWLINE

LOCALS = 
LOCALS += "<div rect='81,0,1937,2812' border='31' bcolor='#C0C0C0' padding='25,50,25'>"
LOCALS += "<br>"
IF 画面表示用変数 == 100
	LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[100] 주거 구역의 기본 방침</font>"
ELSE
	LOCALS += "<button value='100'>[100] 주거 구역의 기본 방침</button>"
ENDIF
LOCALS += "<br>"
LOCALS += "<br>"
; LOCALS += "<button value='998'>[998] システムの説明</button>"
; LOCALS += "<br>"
LOCALS += "<button value='999'>[999] 돌아가기</button>"
LOCALS += "<br>"
LOCALS += "</div>"


LOCALS += "<div rect='2050,0,3875,2812' border='31' bcolor='#C0C0C0' padding='25,50,25'>"
SELECTCASE 画面表示用変数
	CASE 0
		LOCALS += "리조트 전체에 영향을 미치는 조례를 발포합니다. <br>"
		LOCALS += "기본적으로 항목마다 여러 선택지가 있으며, 그중에서 하나를 선택하는 형식입니다. <br>"
		LOCALS += "또한, 형식 전환에 단점이나 비용은 존재하지 않습니다. <br>"
	CASE 100
		LOCALS += 条例関数_居住区の基本方針()
ENDSELECT
LOCALS += "</div>"

HTML_PRINT LOCALS
FOR LOCAL, 0, 30
	PRINTL
NEXT

BINPUT
SELECTCASE RESULT
	CASE 100
		画面表示用変数 = RESULT
	CASE 200 TO 210
		CALLFORM 条例関数_操作_{画面表示用変数}(RESULT)
	CASE 999
		画面表示用変数 = 0
		RETURN -1
ENDSELECT


RESTART

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE380_2_9
;条件に合う場合0（不許可）を返す
;どの条件にも引っかからないなら1（許可）を返す
;一括管理
SIF GLOBAL_COMABLE(380)
	RETURN RESULT
;睡眠中
SIF CFLAG:睡眠
	RETURN 0
;隠密中はダメ
SIF CFLAG:PLAYER:隠密
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM380_2_9
;ここには到達しない


@条例関数_居住区の基本方針()
#FUNCTIONS
#DIMS HTML文字列

HTML文字列 = <br>■주거 구역의 기본 방침<br>
HTML文字列 += "　현재 방침:"
SELECTCASE 条例発布:0
	CASE 0
		HTML文字列 += "자율성에 맡기다<br>"
		HTML文字列 += "　　특별히 아무것도 지시하지 않고, 거주자들의 자유에 맡기는 방침. <br>"
		HTML文字列 += "　　효과: 없음<br>"
	CASE 1
		HTML文字列 += "사진발 중시의 관광 도시<br>"
		HTML文字列 += "　　거리 풍경을 분위기 있게 정돈하고, 경관을 해치는 건물이나 설치물을 금지하는 방침. <br>"
		HTML文字列 += "　　효과: 하루 종료 시의 일반 관광객에 의한 평가가 상승한다.<br>"
	CASE 2
		HTML文字列 += "경제 중시의 상업 도시<br>"
		HTML文字列 += "　　상점 등을 유치하여, 섬 내의 경제를 돌리는 것을 우선하는 방침.<br>"
		HTML文字列 += "　　효과: 하루 종료 시의 리조트의 루피 수지가 플러스일 때,<br>"
	CASE 3
		HTML文字列 += "증가액을 1.2배로 한다.<br>"
		HTML文字列 += "자급 중시의 농업 도시<br>"
		HTML文字列 += "　　식량 증산을 유념하여, 섬 내의 자급률을 올리는 방침.<br>"
ENDSELECT
HTML文字列 += "<br>"

IF 条例発布:0 == 0
		HTML文字列 += "　　효과: 메뉴 개발 시, 요리 랭크 +1. 「BBQ를 한다」 커맨드 강화.<br>"
ELSE
	HTML文字列 += @"<font color='#%カラーパレット_HTML("黄")%'>[200] 자율성에 맡긴다</font>"
ENDIF
HTML文字列 += "<br>"
IF 条例発布:0 == 1
	HTML文字列 += "<button value='200'>[200] 자율성에 맡긴다</button>"
ELSE
	HTML文字列 += @"<font color='#%カラーパレット_HTML("黄")%'>[201] 보기 좋음 중시의 관광 도시</font>"
ENDIF
HTML文字列 += "<br>"
IF 条例発布:0 == 2
	HTML文字列 += "<button value='201'>[201] 보기 좋음 중시의 관광 도시</button>"
ELSE
	HTML文字列 += @"<font color='#%カラーパレット_HTML("黄")%'>[202] 경제 중시의 상업 도시</font>"
ENDIF
HTML文字列 += "<br>"
IF 条例発布:0 == 3
	HTML文字列 += "<button value='202'>[202] 경제 중시의 상업 도시</button>"
ELSE
	HTML文字列 += @"<font color='#%カラーパレット_HTML("黄")%'>[203] 자급 중시의 농업 도시</font>"
ENDIF
HTML文字列 += "<br>"

RETURNF HTML文字列

@条例関数_操作_100(受け渡し数値)
#DIM 受け渡し数値

条例発布:0 = 受け渡し数値 - 200

RETURN 0
