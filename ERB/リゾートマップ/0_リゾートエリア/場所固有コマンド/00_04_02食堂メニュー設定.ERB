
;-------------------------------------------------
;食堂メニュー設定
;-------------------------------------------------

@COMNAME_PLACE_381_0_4
#FUNCTION
TSTR:コマンド名受渡 = 메뉴 설정
RETURNF 1

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_381_0_4
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("単独可能")

@COM_TOOLTIP_381_0_4
LOCALS = <br>■메뉴 설정<br>
LOCALS += "소지하고 있는 메뉴 중, 어떤 것을 식당에 내놓을지 설정하는 커맨드。<br>"
LOCALS += "<br>COM 타입: 단독 가능<br>획득 소스: 환락・정복(일정 조건 시)"
LOCALS += "<br>고유 획득 경험: 없음<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM381_0_4

CALL 食堂メニュー設定処理
RETURN RESULT

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE381_0_4
;条件に合う場合0（不許可）を返す
;どの条件にも引っかからないなら1（許可）を返す
;一括管理
SIF GLOBAL_COMABLE(381)
	RETURN RESULT
;睡眠中
SIF CFLAG:睡眠
	RETURN 0
;隠密中はダメ
SIF CFLAG:PLAYER:隠密
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM381_0_4
