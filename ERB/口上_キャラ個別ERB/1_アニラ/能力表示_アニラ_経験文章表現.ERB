;****************************************
;能力表示画面で、経験表示を切り替えるための関数
;****************************************
;・使い方
;「1」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;コマンド実行時処理
;--------------------------------------------------
@経験文章表現_1(対象キャラ)
#DIM 対象キャラ
;単純にここでPRINT処理を行う
;表示条件なども全部ここで組むこと
;どの変数でも表示条件に組み込めるが、表示場所の関係のため基本的にEXPを条件とすること
;能力表示画面のため、WAIT系は基本的に使わないこと

;例）
IF EXP:対象キャラ:乳首開発 < 50
	PRINTFORML ■%CALLNAME:対象キャラ%의 유두는 아직 풋풋하고, 개발되지 않았다.
ELSEIF EXP:対象キャラ:乳首開発 < 100
	PRINTFORML ■%CALLNAME:対象キャラ%은(는) 유두에서의 쾌락에 익숙해지기 시작한 것 같다……
ELSEIF EXP:対象キャラ:乳首開発 < 200
	PRINTFORML ■%CALLNAME:対象キャラ%의 유두는 살짝 건드리는 것만으로도 감미로운 쾌락을 전해준다.
ELSE
	PRINTFORML ■%CALLNAME:対象キャラ%은(는) 유두를 조금만 만져도 금방 절정해버리는 것 같다……
ENDIF

RETURN 0
