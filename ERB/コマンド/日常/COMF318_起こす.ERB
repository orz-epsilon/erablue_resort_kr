@COMNAME318
#FUNCTIONS
TSTR:コマンド名受渡 = 깨우다

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_318
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("日常")
睡眠中許可 = 1

@COM_TOOLTIP_318
LOCALS = <br>■깨우다<br>
LOCALS += "어떤 이유로 낮에 잠들어 있는 상대를 깨우는 커맨드. <br>아침에는 깨울 수 없다."
LOCALS += "<br>COM 타입: 일상<br>획득 소스: 없음"
LOCALS += "<br>고유 획득 경험: 없음<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM318

CFLAG:TARGET:睡眠 = 0
TCVAR:TARGET:日中睡眠 = 0

RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE318

;実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(318)
	RETURN RESULT
;日中睡眠中のみ
SIF TCVAR:日中睡眠 == 0
	RETURN 0

RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM318
PRINTFORML %CALLNAME:PLAYER%은(는) 잠들어 버린%CALLNAME:TARGET%의 몸을 흔들어 깨웠다.

