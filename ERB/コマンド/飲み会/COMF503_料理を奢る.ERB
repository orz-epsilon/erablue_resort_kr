
@COMNAME503
#FUNCTIONS
TSTR:コマンド名受渡 = 요리를 대접한다

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_503
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("飲み会")

@COM_TOOLTIP_503
LOCALS = <br>요리를 대접한다<br>
LOCALS += "회식에 참가하고 있는 모두에게 요리를 대접하는 커맨드. <br>그 자리에 있는 캐릭터의 명정 게이지가 미량 감소한다. <br>섭취 칼로리 +500kcal."
LOCALS += "<br>COM 타입: 회식<br>획득 소스: 환락"
LOCALS += "<br>고유 획득 경험: 없음<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM503

FOR LOCAL, 1, CHARANUM
	SIF 飲み会TARGET:LOCAL < 1
		BREAK
	BASE:(飲み会TARGET:LOCAL):酩酊 -= RAND:50 + 20
	BASE:(飲み会TARGET:LOCAL):酩酊 = MAX(BASE:(飲み会TARGET:LOCAL):酩酊, 0)
	CALL SOURCE_CALC_歓楽((飲み会TARGET:LOCAL), PLAYER, 200)
	CALL SOURCE_CALC_好感度要素_友情度UP(TARGET, PLAYER, 80)

	TCVAR:(飲み会TARGET:LOCAL):摂取カロリー += 500
NEXT

TCVAR:MASTER:摂取カロリー += 500

TIME += 15


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE503
;会話実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(503)
	RETURN RESULT
SIF TALENT:TARGET:飲食不可
	RETURN 0
SIF IS_FOOD_COURT(CFLAG:PLAYER:現在マップ種別, CFLAG:PLAYER:現在位置) == 0
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM503
PRINTFORM %CALLNAME:PLAYER%은(는) %CALLNAME:TARGET%
SIF GET_飲み会TARGETNUM() > 1
	PRINT 들에게
PRINTL 맛있는 요리를 대접했다.

PRINTFORML （섭취 칼로리: 500kcal）

