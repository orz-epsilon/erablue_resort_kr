
@COMNAME_S60_11
#FUNCTIONS
TSTR:コマンド名受渡 = 허리를 움직인다
SIF MODE_存在判定_完全一致("騎乗位", TARGET, PLAYER)
	TSTR:コマンド名受渡 = 허리를 흔든다


;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_S_60_11
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("Ｖ系_被挿入中")
弱点コマンドカテゴリ = C_ちんこ
オートコマンド許可 = 1
連続オート許可 = 1

@COM_TOOLTIP_S60_11
LOCALS = <br>■허리를 움직이다<br>
LOCALS += "허리를 움직여 상대에게 쾌락을 주는 커맨드. <br>기승위일 때, 「허리를 흔든다」로 변화하며 효과가 상승한다."
LOCALS += "<br>COM 타입：V계or피삽입중<br>획득 소스：쾌C・노출・접촉"
LOCALS += "<br>고유 획득 경험：C성교경험_V삽입(TARGET)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM_S60_11
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
CALL SOURCE_CALC_快Ｖ(PLAYER, TARGET, 400)
CALL SOURCE_CALC_露出(TARGET, PLAYER, 50)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 150)
CALL SOURCE_CALC_快Ｃ(TARGET, PLAYER, 400)
IF MODE_存在判定_完全一致("騎乗位", TARGET, PLAYER)
	CALL SOURCE_CALC_快Ｃ(TARGET, PLAYER, 400)
	CALL SOURCE_CALC_快Ｖ(PLAYER, TARGET, 400)
ENDIF

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
EXP:Ｃ性交経験_Ｖ挿入 ++
EXP:PLAYER:Ｖ性交経験 ++
;同性愛
SIF 同性愛性癖判定(TARGET,PLAYER)
	EXP:TARGET:同性愛経験 += 4
SIF 同性愛性癖判定(PLAYER,TARGET)
	EXP:PLAYER:同性愛経験 += 4

EXP:愛情経験 += TALENT:恋慕 * 2

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE_S60_11
;腸内を締める実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE_S("60_11")
	RETURN RESULT
;挿入系モードじゃない時はアウト
SIF !MODE_存在判定_完全一致("Ｖ挿入系", TARGET, PLAYER)
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM_S60_11
IF MODE_存在判定_完全一致("騎乗位", TARGET, PLAYER)
	PRINTFORML %CALLNAME:PLAYER%는 스스로 허리를 흔들어, %CALLNAME:TARGET%의 음경을 몰아붙이고 있다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%는 %CALLNAME:TARGET%를 받아들이듯 허리를 움직여, 서로 쾌락을 높여가고 있다…
ENDIF

