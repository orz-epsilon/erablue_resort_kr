;-------------------------------------------------
;触手口辱
;触手系コマンド、レベル1
;-------------------------------------------------
@COMNAME102
#FUNCTIONS
TSTR:コマンド名受渡 = 촉수 구강 능욕


;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_102
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("触手系")
;特殊な条件が必要なコマンドなので、弱点コマンドのランダム対象からは外す
;弱点コマンドカテゴリ = B
同時モード許可 = 999


@COM_TOOLTIP_102
LOCALS = <br>■촉수구욕<br>
LOCALS += "캐릭터의 입에 촉수를 넣어 구강 내를 희롱하는 커맨드."
LOCALS += "<br>COM 타입: 촉수계<br>획득 소스: 쾌S・접촉"
LOCALS += "<br>고유 획득 경험: 촉수 조작 경험(PLAYER)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM102
#DIM 快楽基礎値

CALL MODE_CHANGE("触手口辱", PLAYER, TARGET)
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
RCVAR:触手コマンドフラグ = 1

快楽基礎値 = あなた特殊能力:触手変化術 * 3
CALL SOURCE_CALC_快Ｓ(TARGET, PLAYER, 快楽基礎値 * 40)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 快楽基礎値 * 20)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛
SIF 同性愛性癖判定(TARGET,PLAYER)
	EXP:TARGET:同性愛経験 += 1
SIF 同性愛性癖判定(PLAYER,TARGET)
	EXP:PLAYER:同性愛経験 += 1

EXP:PLAYER:触手操作経験 ++
RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE102
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(102)
	RETURN RESULT
SIF MODE_存在判定_ターゲット側("触手口辱", TARGET)
	RETURN 0
;口を何かに使ってるなら無理
SIF TEQUIP:TARGET:口
	RETURN 0
;将来的には許可する予定だけど（ユリウスとか）一旦あなた以外は封印
SIF PLAYER != MASTER
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM102
#DIM 同時キャラ番号
PRINTFORM %CALLNAME:PLAYER%은

IF GETBIT(FLAG:モード管理, モードビット_同時)
	PRINT 수많은 촉수를
	FOR 同時キャラ番号, 0, 同時モード_選択数
		SIF 同時キャラ番号 > 0
			PRINT ,
		PRINTFORM %CALLNAME:(同時モード_選択キャラ:同時キャラ番号)%
	NEXT
	PRINT 의 입속으로 쑤셔 넣고, 구강이나 혀를 유린했다
	RETURN 0
ENDIF

PRINTFORML 자신의 촉수를 %CALLNAME:TARGET%의 입속으로 쑤셔 넣어, 구강이나 혀를 유린했다


