

@COMNAME23
#FUNCTIONS
TSTR:コマンド名受渡 = 시키는 대로

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_23
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ_被挿入中")

@COM_TOOLTIP_23
LOCALS = <br>■なすがまま<br>
LOCALS += "행동하지 않고, 상대방이 원하는 대로 하게 하는 커맨드. <br>5커맨드 동안, 오토 커맨드를 연속 발동시킨다."
LOCALS += "<br>COM 타입: 우후후<br>획득 소스: 없음"
LOCALS += "<br>고유 획득 경험: 없음<br>"
TSTR:ツールチップ受渡 = %LOCALS%


;なすがまま
@COM23
TFLAG:オートコマンド連続発動 = 5
TFLAG:オートコマンド連続発動_なすがまま = 1

TIME += 10
RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE23
;なすがまま実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(23)
	RETURN RESULT
;指示モード・同時モードだと発動しない
SIF FLAG:モード管理 != 0
	RETURN 0
;オートコマンド禁止だと発動しない
SIF GETBIT(FLAG:オートコマンド切り替えオプション, 1) && GETBIT(FLAG:オートコマンド切り替えオプション, 3)
	RETURN 0
RETURN 1


;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM23
PRINTFORML %CALLNAME:PLAYER%는 %CALLNAME:TARGET%의 뜻대로 된다…

