;-------------------------------------------------
;ローター
;道具使用コマンド、レベル1
;-------------------------------------------------
@COMNAME39
#FUNCTIONS
TSTR:コマンド名受渡 = 로션

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_39
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("道具系")



@COM_TOOLTIP_39
LOCALS = <br>■로션<br>
LOCALS += "로션을 사용하여 윤활도를 상승시키는 커맨드."
LOCALS += "<br>COM 타입: 도구계<br>획득 소스: 액체·접촉"
LOCALS += "<br>고유 획득 경험: 도구 사용 경험(PLAYER)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM39
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

CALL SOURCE_CALC_液体(TARGET, PLAYER, 20000)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 60)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
ITEM:40 -= 1
EXP:PLAYER:道具使用経験 ++
RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE39
;ローター実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(39)
	RETURN RESULT
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:40 == 0 && NOITEM == 0
	RETURN 0

;下半身着衣
;前鎧、めくれない服があると無理
SIF TEQUIP:下腹部前鎧あり
	RETURN 0
SIF TEQUIP:下半身服あり && (!TEQUIP:スカートめくり可 && !TEQUIP:ずらし可)
	RETURN 0

;使用者が指を何かに使ってるなら無理
SIF TEQUIP:PLAYER:指
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM39
PRINTFORM %CALLNAME:PLAYER%는
IF TEQUIP:下タイツあり && !TEQUIP:下タイツ破れ
	PRINTFORM %CALLNAME:TARGET%의
	PRINTL 하반신에 타이츠 위로 로션을 떨어뜨렸다…
	RETURN 0
ENDIF
IF TEQUIP:下半身下着あり > 0
	PRINTFORM 속옷 위로
ENDIF
PRINTFORM %CALLNAME:TARGET%의
IF GETBIT(TALENT:TARGET:性別, 1)
	;オトコ
	PRINTL 딱딱하게 발기한 페니스에 로션을 떨어뜨렸다…
ELSE
	PRINTL 사타구니에 로션을 떨어뜨렸다…
ENDIF

