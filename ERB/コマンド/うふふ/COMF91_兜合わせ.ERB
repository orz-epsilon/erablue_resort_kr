;===================================================
;兜合わせ
;===================================================
@COMNAME91
#FUNCTIONS
TSTR:コマンド名受渡 = 투구 맞대기

;許可時の特別文章はここに記入
IF TFLAG:オートコマンドフラグ >= 4000
	;反転時、つまりあなたがタチ
	許可取り文章 = %CALLNAME:TARGET%는 %CALLNAME:PLAYER%에게 [투구 맞대기]를 해줬으면 하는 것 같다…
ELSE
	許可取り文章 = %CALLNAME:PLAYER%는 %CALLNAME:TARGET%와 [투구 맞대기]를 하고 싶은 것 같다…
ENDIF


;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_91
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
弱点コマンドカテゴリ = C_ちんこ_同性
オートコマンド許可 = 3
反転オート許可 = 3


;===================================================
;ツールチップ文章
;===================================================
@COM_TOOLTIP_91
TSTR:ツールチップ受渡 '= "<br>■투구 맞대기<br>"
TSTR:ツールチップ受渡 += "서로의 페니스 끝을 비벼 쾌락을 주고받는 커맨드. <br>투구 맞대기 모드가 되어, 모드를 변경하거나 성행위를 종료할 때까지 지속적으로 특정 소스가 발생한다."
TSTR:ツールチップ受渡 += "<br>COM 타입: 우후후<br>획득 소스: 쾌C・노출・접촉・불결"
TSTR:ツールチップ受渡 += "<br>고유 획득 경험: 없음<br>"


;===================================================
;コマンド実行処理
;===================================================
@COM91
JUMP 疑似性交COM("兜合わせ", 200, 200)


;===================================================
;実行許可判定
;===================================================
@CAN_COM91(ARG)
JUMP 疑似性交CAN_COM(ARG, "兜合わせ", 16)


;===================================================
;実行可能判定
;===================================================
@COM_ABLE91
;兜合わせ実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(91)
	RETURN RESULT

;PLAYER側の判定
SIF !疑似性交COM_ABLE_男性キャラ(PLAYER)
	RETURN 0
;ある程度勃起してないとダメ(PLAYER)
SIF BASE:PLAYER:勃起 < 1000
	RETURN 0
;TARGET側の判定
SIF !疑似性交COM_ABLE_男性キャラ(TARGET)
	RETURN 0
;ある程度勃起してないとダメ(TARGET)
SIF BASE:TARGET:勃起 < 1000
	RETURN 0
RETURN 1


;===================================================
;実行時メッセージ
;===================================================
@MESSAGE_COM91
PRINTFORML %CALLNAME:PLAYER%는 %CALLNAME:TARGET%와 마주보고, 페니스의 요도구와 요도구를 입맞추듯이 서로 겹쳐 맞대자, 허리를 흔들며 문지르기 시작했다…
