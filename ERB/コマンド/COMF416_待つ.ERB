;待つ（隠密状態のみ）
@COMNAME416
#FUNCTIONS
TSTR:コマンド名受渡 = 기다린다

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_416
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("隠密")

@COM_TOOLTIP_416
LOCALS = <br>■待つ<br>
LOCALS += "숨은 채 그 자리에서 기다리는 커맨드.<br>15분 소비한다."
LOCALS += "<br>COM 타입: 은밀<br>획득 소스: 없음"
LOCALS += "<br>고유 획득 경험: 없음<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM416
TIME += 15


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE416
;実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(416)
	RETURN RESULT
;隠密時のみ
SIF !CFLAG:MASTER:隠密
	RETURN 0
;うふふ中は封印
SIF TFLAG:調教モード == 2
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
