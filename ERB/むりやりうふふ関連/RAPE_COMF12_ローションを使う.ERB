;-------------------------------------------------
;ローションを使う
;道具使用コマンド、レベル1
;-------------------------------------------------
@RAPE_COMNAME12
#FUNCTIONS
TSTR:コマンド名受渡 = 로션을 사용한다

;===================================================
;コマンドタイプ
;===================================================
@RAPE_COMTYPE_12
#FUNCTION
TFLAG:コマンドタイプ受渡 = RAPE_COMTYPE("道具系")



@RAPE_COM_TOOLTIP_12
LOCALS = <br>■로션을 사용한다<br>
LOCALS += "가지고 있는 로션을 사용하여 윤활도를 상승시키는 커맨드."
LOCALS += "<br>RAPE_COM타입：도구 계열<br>획득 소스：액체・접촉"
LOCALS += "<br>고유 획득 경험：도구 사용 경험(당신)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@RAPE_COM12
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;女あなたの逆レの時潤滑不足になっても嫌なので互いに加算

CALL SOURCE_CALC_液体(TARGET, PLAYER, 20000)
CALL SOURCE_CALC_液体(PLAYER, TARGET, 20000)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 60)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
ITEM:40 -= 1
EXP:PLAYER:道具使用経験 ++
RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@RAPE_COM_ABLE12
;ローション実行判定
;一括管理
SIF GLOBAL_RAPE_COMABLE(12)
	RETURN RESULT
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:40 == 0 && NOITEM == 0
	RETURN 0

;下半身着衣
;前鎧、めくれない服があると無理
SIF TEQUIP:下腹部前鎧あり
	RETURN 0
SIF TEQUIP:下半身服あり && (!TEQUIP:スカートめくり可 && !TEQUIP:ずらし可)
	RETURN 0
	
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@RAPE_MESSAGE_COM12
PRINTFORM %CALLNAME:PLAYER%은(는)
IF TEQUIP:下タイツあり && !TEQUIP:下タイツ破れ
	PRINTFORM %CALLNAME:TARGET%의
	PRINTFORML 하반신에 타이츠 위로 로션을 듬뿍 발랐다…
	RETURN 0
ENDIF
IF TEQUIP:下半身下着あり > 0
	PRINTFORM 속옷 위로
ENDIF
PRINTFORM %CALLNAME:TARGET%의
IF GETBIT(TALENT:TARGET:性別, 1)
	;オトコ
	PRINTFORML 단단하게 발기한 페니스에 로션을 듬뿍 발랐다…
ELSE
	PRINTFORML 서로의 사타구니에 로션을 듬뿍 발랐다…
ENDIF

