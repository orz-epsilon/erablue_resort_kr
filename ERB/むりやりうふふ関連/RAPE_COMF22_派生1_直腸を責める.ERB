
@RAPE_COMNAME_S22_1
#FUNCTIONS
TSTR:コマンド名受渡 = 직장을 괴롭히다
;男・ふたなり相手だと前立腺責めになる
SIF GETBIT(TALENT:TARGET:性別, 2)
	TSTR:コマンド名受渡 = 전립선을 괴롭히다

;===================================================
;コマンドタイプ
;===================================================
@RAPE_COMTYPE_S_22_1
#FUNCTION
TFLAG:コマンドタイプ受渡 = RAPE_COMTYPE("Ａ系")

@RAPE_COM_TOOLTIP_S22_1
LOCALS = <br>■직장을 괴롭히다<br>
LOCALS += "장내 입구 부근에 페니스로 강한 자극을 주는 커맨드. <br>상대가 남자·후타나리일 때, [전립선을 괴롭히다]로 변화한다. <br>A삽입계 모드일 때만 실행 가능."
LOCALS += "<br>RAPE_COM 타입: A계<br>획득 소스: 쾌A·접촉·고통"
LOCALS += "<br>고유 획득 경험: A성교 경험(TARGET)·전립선 개발(TARGET·일정 조건 시)·C성교 경험_A삽입(PLAYER)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@RAPE_COM_S22_1
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

;ペニスがあるなら前立腺もある
IF GETBIT(TALENT:TARGET:性別, 1)
	RCVAR:前立腺開発フラグ = 3
ENDIF
CALL SOURCE_CALC_快Ａ(TARGET, PLAYER, 500)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 150)
CALL SOURCE_CALC_快Ｃ(PLAYER, TARGET, 500)

;苦痛
LOCAL = 200 * PAIN_CHECK_A(TARGET) / 1000
CALL SOURCE_CALC_苦痛(TARGET, PLAYER, LOCAL)


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
EXP:PLAYER:Ｃ性交経験_Ａ挿入 ++
EXP:Ａ性交経験 ++

;同性愛　レズなら戻り値1ゲイなら戻り値2
IF HETEROSEX(TARGET,PLAYER)
	EXP:同性愛経験 += 4
	EXP:PLAYER:同性愛経験 += 4
ENDIF

EXP:愛情経験 += TALENT:恋慕 * 2

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@RAPE_COM_ABLE_S22_1
;入口を責める実行判定
;一括管理
SIF GLOBAL_RAPE_COMABLE_S("22_1")
	RETURN RESULT
;Ａ挿入系モードじゃない時はアウト
SIF MODE_確認_完全一致("Ａ挿入系", PLAYER, TARGET) < 0
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@RAPE_MESSAGE_COM_S22_1
#DIM 同時キャラ番号
PRINTFORM %CALLNAME:PLAYER%은(는)


IF GETBIT(TALENT:TARGET:性別, 1)
	PRINTFORM %CALLNAME:TARGET%의 오돌토돌한 전립선을 끈질기게 괴롭혔다
ELSE
	PRINTFORM %CALLNAME:TARGET%의 직장을 꼼꼼히 파헤쳤다
ENDIF

