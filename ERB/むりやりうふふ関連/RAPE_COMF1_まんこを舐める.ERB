;-------------------------------------------------
;まんこを舐める
;愛撫系コマンド　レベル1
;-------------------------------------------------
@RAPE_COMNAME1
#FUNCTIONS
TSTR:コマンド名受渡 = 보지를 핥다


;===================================================
;コマンドタイプ
;===================================================
@RAPE_COMTYPE_1
#FUNCTION
TFLAG:コマンドタイプ受渡 = RAPE_COMTYPE("うふふ")


@RAPE_COM_TOOLTIP_1
LOCALS = <br>■보지를 핥다<br>
LOCALS += "상대의 가랑이에 얼굴을 묻고, 클리토리스를 중심으로 혀로 자극하며 즐긴다. <br>동의를 얻지 못했으므로 「식스나인」으로는 파생되지 않는다. <br>실행자가 [혀놀림][고양이 혀]를 가지고 있을 경우, 효과가 증감한다."
LOCALS += "<br>RAPE_COM 타입: 우후후<br>획득 소스: 쾌감C・노출・접촉・액체"
LOCALS += "<br>고유 획득 경험: 구강성교 경험(당신)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@RAPE_COM1
;-------------------------------------------------
;モードの設定
;-------------------------------------------------

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

LOCAL = 150
;舌を使う調教では、調教者の舌使いによって、快感のソース増加
SIF TALENT:PLAYER:舌使い
	TIMES LOCAL , 1.50
;猫舌では減少
SIF TALENT:PLAYER:猫舌
	TIMES LOCAL , 0.75
CALL SOURCE_CALC_快Ｃ(TARGET, PLAYER, LOCAL)

CALL SOURCE_CALC_液体(TARGET, PLAYER, 60)
CALL SOURCE_CALC_露出(TARGET, PLAYER, 80)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 100)

;下タイツがある場合は破く。
IF TEQUIP:下タイツあり == 1
	TEQUIP:下タイツ破れ = 1
	RFLAG:コマンド結果受渡し変数 = 1
ENDIF
;前張りがあったら剥がす
IF TEQUIP:下半身下着あり == 2
	TEQUIP:下半身下着あり = 0
	RFLAG:コマンド結果受渡し変数２ = 1
ENDIF
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛　レズなら戻り値1ゲイなら戻り値2
IF HETEROSEX(TARGET,PLAYER)
	EXP:同性愛経験 += 3
	EXP:PLAYER:同性愛経験 += 3
ENDIF


;調教者が猫舌持ちの場合は、調教者に嗜虐快楽経験を追加
SIF TALENT:PLAYER:猫舌
	EXP:PLAYER:嗜虐快楽経験 += 1
;口淫経験
EXP:PLAYER:口淫経験 ++

CALL 初体験日登録処理(PLAYER, TARGET, "ファーストキス", DAY * 1440 + TIME, GETPLACENAME(CFLAG:PLAYER:現在マップ種別, CFLAG:PLAYER:現在位置), "クリトリス", "", 1)
IF RESULT
	CALL 履歴データベース登録(CFLAG:PLAYER:人物番号, @"<font color='#%カラーパレット_HTML("薄ピンク")%'>%CALLNAME:TARGET%의 [크리토리스]에 첫 키스를 바쳤다.</font><img src='えっちハート'>","うふふ")
ENDIF

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@RAPE_COM_ABLE1
;クンニ実行判定
;一括管理
SIF GLOBAL_RAPE_COMABLE(1)
	RETURN RESULT
;クリがない場合駄目
SIF TALENT:TARGET:Ｃ感度 == -2
	RETURN 0
;女性器がないとダメ
SIF GETBIT(TALENT:TARGET:性別, 0) == 0
	RETURN 0
;下半身着衣
;前鎧、めくれない服があると無理
SIF TEQUIP:下腹部前鎧あり
	RETURN 0
SIF TEQUIP:下半身服あり && (!TEQUIP:スカートめくり可 && !TEQUIP:ずらし可)
	RETURN 0

	
;シックスナインには派生しない。処理に自信がないのでフェラ中で禁止をかける。
IF MODE_確認_完全一致("フェラチオ系", TARGET, PLAYER) > -1
	RETURN 0
ELSE
	;口を何かに使ってる・届かないなら無理
	SIF TEQUIP:PLAYER:口
		RETURN 0
	;クリを何かに使ってる・届かないなら無理
	SIF TEQUIP:TARGET:クリトリス
		RETURN 0
ENDIF
;挿入系時は個別にOFF
SIF MODE_確認_完全一致("Ｖ挿入系", PLAYER, TARGET) > -1 || MODE_確認_完全一致("Ａ挿入系", PLAYER, TARGET) > -1
	RETURN 0
SIF MODE_確認_完全一致("Ｖ挿入系", TARGET, PLAYER) > -1 || MODE_確認_完全一致("Ａ挿入系", TARGET, PLAYER) > -1
	RETURN 0

;オートコマンド中の制限
IF FLAG:オートコマンド判定中フラグ
	SIF 初体験済みチェック("ファーストキス", PLAYER) == 0 
		RETURN 0
ENDIF

RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@RAPE_MESSAGE_COM1

PRINTFORM %CALLNAME:PLAYER%은(는),
	IF RFLAG:コマンド結果受渡し変数 == 1
		PRINTFORM 신고 있던 타이츠를 강제로%TEXTR("찢어발겨/찢어")% 노출시켰다
	ELSEIF RFLAG:コマンド結果受渡し変数２ == 1
		PRINTFORM 붙어 있던 앞가리개를 강제로 벗겨 노출시켰다
	ELSEIF !TEQUIP:下半身服あり && TEQUIP:下半身下着あり > 0
		PRINTFORM 속옷을 벗겨 노출시켰다
	ELSEIF TEQUIP:ずらし可
		PRINTFORM 옷을 벗겨 노출시켰다
	ELSEIF TEQUIP:スカートめくり可
		PRINTFORM 옷을 걷어 올려 노출시켰다
	ENDIF
PRINTFORM %CALLNAME:TARGET%의 은밀한 입술을 빨아대며,
;ふたなり＋欲情のPLAMLV3以上
IF GETBIT(TALENT:性別, 1) && PALAM:欲情 >= PALAMLV:3
	PRINTFORM 강간당하고 있음에도 불구하고 흥분으로 솟아오른 페니스를 손으로 주무르면서,
;ふたなり
ELSEIF GETBIT(TALENT:性別, 1)
	PRINTFORM 후타나리 자지를 손으로 만지작거리면서,
ENDIF
;欲情のPALAMLV3以上
IF GETBIT(TALENT:性別, 1)
;ふたなりの場合クリはちんぽだからしゃぶれないんじゃないか？
	PRINTFORM 애액으로 젖은 음순을 핥아 빨았다
ELSEIF PALAM:欲情 >= PALAMLV:3
	PRINTFORM 강간당하고 있음에도 불구하고 욕정으로 팽팽하게 솟아오른 클리토리스를%TEXTR("빨아올렸다/혀로 핥아댔다")%
ELSE
	PRINTFORM 클리토리스를%TEXTR("빨아올려/혀로 핥아대며")% 반응을 즐겼다
ENDIF
PRINTFORML …

