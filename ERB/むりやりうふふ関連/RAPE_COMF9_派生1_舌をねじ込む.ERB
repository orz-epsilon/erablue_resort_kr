
@RAPE_COMNAME_S9_1
#FUNCTIONS
TSTR:コマンド名受渡 = 혀를 비집어 넣다

;===================================================
;コマンドタイプ
;===================================================
@RAPE_COMTYPE_S_9_1
#FUNCTION
TFLAG:コマンドタイプ受渡 = RAPE_COMTYPE("うふふ_被挿入中")


@RAPE_COM_TOOLTIP_S9_1
LOCALS = <br>■혀를 비집어 넣다<br>
LOCALS += "키스하는 상대의 입안에 혀를 비집어 넣어 얽히게 한다. <br>딥 키스 모드로 이행. <br>[혀놀림][고양이 혀]에 따라 효과가 증감."
LOCALS += "<br>RAPE_COM 타입: 우후후or피삽입중or성희롱<br>획득 소스: 노출・접촉・쾌S・불결・고통(일정 조건 시)"
LOCALS += "<br>고유 획득 경험: 키스 경험(당신・TARGET)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@RAPE_COM_S9_1
;-------------------------------------------------
;モードの設定
;-------------------------------------------------
CALL MODE_CHANGE("ディープキス", PLAYER, TARGET)
;-------------------------------------------------
;RAPE_COM別FLAGの設定
;-------------------------------------------------
;奉仕コマンドフラグ
RCVAR:TARGET:奉仕コマンドフラグ = 1
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------


CALL SOURCE_CALC_露出(TARGET, PLAYER, 50)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 250)
CALL SOURCE_CALC_快Ｓ(TARGET, PLAYER, 250)
CALL SOURCE_CALC_快Ｓ(PLAYER, TARGET, 250)
CALL SOURCE_CALC_好感度要素_恋心度UP(TARGET, PLAYER, 150)

CALL SOURCE_CALC_不潔(TARGET, PLAYER, 50)


;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------

;舌を使う調教では、調教者の舌使いによって、快感のソース増加
IF TALENT:PLAYER:舌使い
	CALL SOURCE_CALC_接触(TARGET, PLAYER, 50)
	CALL SOURCE_CALC_快Ｓ(TARGET, PLAYER, 150)
ENDIF

IF TALENT:TARGET:舌使い
	CALL SOURCE_CALC_快Ｓ(PLAYER, TARGET, 150)
ENDIF

;猫舌では減少
SIF TALENT:PLAYER:猫舌
	CALL SOURCE_CALC_苦痛(TARGET, PLAYER, 20)


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛　レズなら戻り値1ゲイなら戻り値2
IF HETEROSEX(TARGET,PLAYER)
	EXP:同性愛経験 += 3
	EXP:PLAYER:同性愛経験 += 3
ENDIF


SIF TALENT:恋慕
	EXP:愛情経験 += 1

SIF TALENT:TARGET:猫舌
	EXP:嗜虐快楽経験 += 1

EXP:キス経験 ++
EXP:PLAYER:キス経験 ++
RETURN 1


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@RAPE_COM_ABLE_S9_1
;舌を入れる実行判定
;一括管理
SIF GLOBAL_RAPE_COMABLE_S("9_1")
	RETURN RESULT
;ボールギャグ使用中は不可
; SIF TEQUIP:ボールギャグ
; 	RETURN 0
;猫舌の調教者の場合は技巧Lv2以上必要
SIF TALENT:PLAYER:猫舌 && ABL:PLAYER:技巧 < 2
	RETURN 0
;キスモードじゃない時はアウト
SIF MODE_確認_完全一致("キス", PLAYER, TARGET) < 0 && MODE_確認_完全一致("キス", TARGET, PLAYER) < 0
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@RAPE_MESSAGE_COM_S9_1

PRINTFORML %CALLNAME:PLAYER%은(는) %CALLNAME:TARGET%의 입술 틈새%TEXTR("에 혀를 비집어 넣고/를 혀로 벌려")%, %CALLNAME:TARGET%의 %TEXTR("구강을 구석구석 핥아 돌았다/혀의 감촉을 만끽했다/")%…

;朦朧を前提としているので
IF CFLAG:TARGET:発情期フラグ < 0
	PRINTFORML 억지로 범하고 있음에도 불구하고, 발정으로 몽롱해진 %CALLNAME:TARGET%은(는) 무의식적으로 %CALLNAME:PLAYER%에게 아양을 떨며, 느릿느릿 스스로 혀를 얽히게 하고 있다…
ELSEIF PALAM:欲情 >= PALAMLV:4
	PRINTFORML 억지로 범하고 있음에도 불구하고, 욕정으로 몽롱해진 %CALLNAME:TARGET%은(는) 무의식적으로 %CALLNAME:PLAYER%에게 아양을 떨며, 쾌락을 찾아 스스로 혀를 얽히게 하고 있다…
ENDIF
