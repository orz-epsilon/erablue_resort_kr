;-------------------------------------------------
;息を整える
;コミュニケーション系コマンド、レベル1
;-------------------------------------------------
@RAPE_COMNAME10
#FUNCTIONS
TSTR:コマンド名受渡 = 숨을 고르다


;===================================================
;コマンドタイプ
;===================================================
@RAPE_COMTYPE_10
#FUNCTION
TFLAG:コマンドタイプ受渡 = RAPE_COMTYPE("うふふ_被挿入中")


@RAPE_COM_TOOLTIP_10
LOCALS = <br>■숨을 고르다<br>
LOCALS += "성행위 중, 현 상태를 유지하면서 호흡을 가다듬는다. 즉, 「아무것도 하지 않는다」<br>장비 아이템이나 모드 등의 지속적인 쾌락은 발생한다."
LOCALS += "<br>RAPE_COM타입: 우후후or피삽입 중<br>획득 소스: 없음"
LOCALS += "<br>고유 획득 경험: 없음<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@RAPE_COM10
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------



RETURN 1
;放置プレイは後で考える
;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------

;-------------------------------------------------
;実行判定
;-------------------------------------------------

@RAPE_COM_ABLE10
;何もしない
;一括管理
SIF GLOBAL_RAPE_COMABLE(10)
	RETURN RESULT
RETURN 1


;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@RAPE_MESSAGE_COM10

PRINTFORML %CALLNAME:PLAYER%은(는)
IF MODE_確認_完全一致("Ｖ挿入系", PLAYER, TARGET) > -1 || MODE_確認_完全一致("Ａ挿入系", PLAYER, TARGET) > -1 || MODE_確認_完全一致("Ｖ挿入系", TARGET, PLAYER) > -1 || MODE_確認_完全一致("Ａ挿入系", TARGET, PLAYER) > -1
	PRINTFORM %CALLNAME:TARGET%을(를) 범하고 이어진 채, 흥분으로 거칠어진 호흡을 가다듬고, 허리를 흔들며 한숨 돌렸다…
ELSE
	PRINTFORM %CALLNAME:TARGET%을(를) 억누른 채, 흥분으로 너무 거칠어진 호흡을 가다듬었다…
ENDIF


